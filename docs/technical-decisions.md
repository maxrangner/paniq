# Technical decisions

## Chosen foundation

| Area | Decision | Reason |
| --- | --- | --- |
| Engine | Unity 6.3 LTS (`6000.3.24f1`) | Supported Windows workflow, C#, mature 3D tools, and a future ECS path. |
| Rendering | URP | A scalable Unity rendering path suitable for desktop and later mobile quality profiles. |
| Initial runtime model | GameObjects and C# | Keeps the prototype approachable and fast to iterate. |
| Input | Unity Input System | Keeps device input separate from game intent. |
| Tests | Unity Test Framework | Supports edit-mode logic tests and play-mode scene-flow tests. |
| Version control | Git | Text Unity assets are tracked; LFS waits for large source media. |

## Prototype decision: fire-reaction bring-up

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| Scenario data | One `FireReactionScenario` ScriptableObject with an explicit seed, stable agent IDs, and fixed spatial values | It gives the first prototype repeatable starting data without putting mutable run state in the Unity scene. [Unity ScriptableObject manual](https://docs.unity3d.com/6000.3/Documentation/Manual/class-ScriptableObject.html) | The game needs multiple scenario variants or a designer-facing authoring workflow. |
| Simulation cadence | One `FixedUpdate` runner at the existing 0.02-second fixed step | The fire and agent rules advance in a predictable order even when the game renders at a different frame rate. [Unity fixed updates](https://docs.unity3d.com/6000.3/Documentation/Manual/fixed-updates.html) | A replay-compatibility review approves a different fixed step. |
| Crowd movement (revised 2026-09-18) | Integer steering with whole-degree headings: agents turn and accelerate gradually toward a goal, blended with pushes away from people within 0.8 m and walls within 0.6 m. Occupancy checks and the rotating resolver are unchanged; the swept-circle test is now exact for moves in any direction. No Unity physics or navigation. | Owner playtest: four-direction movement read as robotic and panicked agents ran into walls and stayed there. Steering gives curved, eased motion while staying replayable. | A prototype stone needs obstacles or routes that local steering cannot handle; then consider AI Navigation. |
| Integer trigonometry | Fixed 91-entry sine table (scaled by 10,000), an integer binary-search heading function, and integer square root in `IntegerMath` | Free headings need sine and cosine, but floating-point results must not decide outcomes. A table is small, exact, and tested against `Math.Sin`. | Headings need finer than one-degree resolution. |
| Fire reaction and facing | Integer-only forward 90-degree, 3 m vision cone around the agent's current heading, tested against every burning cell | Seeing danger in front of an agent reads more clearly than an invisible radius. | A prototype stone needs occlusion, smoke, or sound direction. |
| Calm behaviour defaults | Per-agent walk pace from the speed trait, 20–32 mm/tick (1.0–1.6 m/s) ±2 (was seeded 22–30 before personalities), turn rate 4–7°/tick, acceleration 2 mm/tick²; decisions every 60–200 ticks between strolling, standing, glancing around, and standing near someone | Small independent decisions read as people rather than random motion; normal human walking pace is about 1.3 m/s. Chosen on the owner's behalf. | Playtesters say the crowd looks too busy, too idle, or too uniform. |
| Panic behaviour defaults | Sprint from the speed trait, 60–110 mm/tick (3–5.5 m/s) ±5 (was seeded 70–100 before personalities), turn rate 10–16°/tick, acceleration 8 mm/tick²; re-decide every 20–60 ticks by scoring 8 random escape spots; 35% chance of a 30–70° swerve; 12% chance of a 5–15 tick hesitation; follow nearby runners at 35% weight; flee directly within 1.5 m of fire; give up a blocked direction after 12 ticks | The owner asked for visible panic: faster running, zig-zags, and changing decisions mid-run. Measured in a headless run of the default seed: about 3.3 m/s average and about two sharp (>30°) direction changes per second in the first 20 s. Chosen on the owner's behalf. | Playtesters find panic unreadable or too twitchy; lower the swerve chance or lengthen the decision interval first. |
| Maximum step distance | Raised from 60 to 120 mm per tick | The panic sprint needs up to 100 mm per tick. It is still well under the 500 mm agent diameter, so swept contact remains meaningful. | Faster movement is needed. |
| Alert propagation (revised 2026-09-18) | A yell is a simulation "sound" with a position and two reaches: calm people within 2.5 m understand it and become alert with their own seeded reaction delay; calm people within 6 m only hear it and turn to look (`AgentNoticedSound`). Runners also shout every 2–5 s while fleeing. | The owner asked for sound as a system indication that makes people turn toward it. Two reaches let a yell spread attention further than alarm, without real audio, scene physics or object references. Chosen on the owner's behalf. | A prototype stone needs walls that block sound or real audio. |
| Alert expression (revised 2026-09-18) | Alerted listeners turn toward what alarmed them: visual alerts face the nearest burning point, yell and bump alerts face where the noise came from | A heard warning still does not pretend the listener can see the fire, but turning toward the yeller often lets them see it themselves. | The prototype needs richer memory. |
| Hearing defaults | Fire crackles within 3.5 m of the nearest burning square; a collision or trip thud carries 3 m. A calm person who hears a noise turns toward it for 1–2.5 s (`Investigating`), turning at their panic turn rate, and after 0.5 s edges toward it at half walking pace if it is over 2 m away. Only calm people react to noises. | Hearing reaches just past the 3 m sight range, so someone with their back to the fire turns around, finds nothing in sight at the edge of hearing, and creeps closer until they see it. Calm-only keeps the rules readable. Chosen on the owner's behalf. | Playtesters find the room too twitchy (shorten the reach) or fire too easy to miss (lengthen it). |
| Panic temperaments | Dealt like a shuffled deck, not rolled independently: 15% freeze for good, 30% freeze for 2–6 s (or until fire is within 1.5 m) then run, the rest run. Ten people: 2, 3 and 5. Only who gets which comes from the seed. | The owner asked for some people to freeze in fear, some briefly and some for good. Independent rolls left the default seed with no permanent freezer at all, so the owner might never see it. Chosen on the owner's behalf. | The mix feels wrong in play, or scenarios need authored per-person temperaments. |
| Collisions and falls | A fleeing runner blocked by an upright person runs into them when their closing speed is at least 50 mm/tick (2.5 m/s). At 100 mm/tick (5 m/s) or more both are knocked down for 1.2–2.8 s and take 0.5 s to get up; below that both stagger for 0.2–0.4 s with a 25–50° jolt. A runner blocked by someone on the floor steps around them if any side-step works and otherwise trips over them. Nobody ever overlaps: a collision is a move that did not happen, resolved after movement in the order it was recorded. | The owner asked for panic to feel frantic, with fast collisions putting both people on the floor. 5 m/s means any head-on pair of runners, or a top-speed sprinter into someone standing. Measured headless over seeds 40–46: 13–31 collisions and 4–12 knockdowns in the first 20 s. Chosen on the owner's behalf. | Playtesters find the floor too crowded (raise the knockdown speed) or collisions too rare (lower panic people-avoidance from 50%). |
| Tripping | A runner going at least 60 mm/tick (3 m/s) has a 4% chance per panic decision to trip, doubled while zig-zagging; down for 0.8–2 s. Tripping over a fallen person uses the same fall time. | The owner asked that erratic panicking people sometimes trip. An earlier draft tripped runners over anyone on the floor even when they could step around, which gave up to 28 trips in 20 s; stepping around first brought it to 2–12. Chosen on the owner's behalf. | Trips look too common or too rare in play. |
| Icon drawing | Icons are `LineRenderer` shapes and bold `TextMesh` text on a per-person anchor that always faces the camera, drawn with the built-in unlit `Sprites/Default` shader so colours stay bright and can fade. | Icons were children of the capsule, so they spun with it and would tip over when it fell. Line shapes can draw the arcs and snowflake the owner asked for without adding art assets or packages. [LineRenderer](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/LineRenderer.html) | Icons need authored art. `Sprites/Default` is already in **Project Settings > Graphics > Always Included Shaders**, so standalone builds keep it; the code falls back to URP Unlit if it is ever removed. |
| Prototype fire shape (revised 2026-09-18) | A 24 × 24 grid of 500 mm cells. The fire starts in one seeded cell; each burning cell lights one random unburnt 4-neighbour every 40–120 ticks and never goes out. Each ignition is a `FireSpread` event caused by the cell that lit it. Shown as a glowing tile plus 2–3 small animated cubes per cell. | The owner asked for fire made of small animated cubes that spreads from itself. A grid makes "spreads from itself" literal and keeps contact checks cheap and exact. The room fills in about 40–45 s. Chosen on the owner's behalf. | A prototype stone needs fire that burns out, spreads by material, or is blocked by walls. |
| Fire cube presentation | GameObject cubes with one shared emissive URP Lit material, recoloured per cube through a `MaterialPropertyBlock`; variation from a hash of the cell position | Up to about 1,700 cubes is fine as plain GameObjects for a prototype, and a property block avoids one material copy per cube. [MaterialPropertyBlock](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/MaterialPropertyBlock.html) | Profiling a standalone build shows the fire display costs noticeable frame time; then move to GPU instancing (`Graphics.RenderMeshInstanced`). |
| Replay compatibility | `SimulationCompatibilityVersion` 26 → 27 and `ContentRevision` 34 → 35 (2026-09-22) for backing out of a queue at the only way out. Six of the ten fingerprints re-recorded; four happen not to change. Earlier: 25 → 26 and `ContentRevision` 33 → 34 (2026-09-21) for this playtest round: eagerness to take a way out, the news that a door has opened, tables that tip over, stop-drop-and-roll, the sweeping jet and the chair scoot. All ten fingerprints re-recorded. Earlier: 24 → 25 and `ContentRevision` 32 → 33 (2026-09-21) for fighting only the fire in your own room. Seven of the ten fingerprints re-recorded; three happen not to change. Earlier: 23 → 24 and `ContentRevision` 31 → 32 (2026-09-21) for the single way out of the building and the sideways step round an obstruction. All ten fingerprints re-recorded. Earlier: 22 → 23 and `ContentRevision` 30 → 31 (2026-09-21) for the prototype clean-up: the smaller meeting room and wider corridor, the meeting that starts seated, things that rest on other things, spin from the knock that caused it, popping laptops, noticing an extinguisher put down, remembering a door you shut yourself, orders beating following, and the dragger stuck count. All ten fingerprints re-recorded. Earlier: 21 → 22 and `ContentRevision` 29 → 30 (2026-09-21) for the cruelty-and-powers batch: shoving, fire alarms, starting possessions, evil-only door slamming, breaking and popping, wedged doorways, influence and the four cards. All eight fingerprints re-recorded, and two new ones added that play every card, so the player-command path is covered by replay as well as by its own tests. Earlier: 20 → 21 and 28 → 29 (2026-09-20) for leadership; fingerprints re-recorded. Earlier: 19 → 20 and 27 → 28 (2026-09-20) for fire extinguishers; fingerprints re-recorded. Earlier: 18 → 19 and 26 → 27 (2026-09-20) for sitting on chairs; fingerprints re-recorded. Earlier: 17 → 18 and 25 → 26 (2026-09-20) for the office's loose things; fingerprints re-recorded. Earlier: 16 → 17 and 24 → 25 (2026-09-20) for the corridor, meeting room and ten more people; fingerprints re-recorded. Earlier: 15 → 16 and 23 → 24 (2026-09-19) for rooms and doors as a graph; fingerprints re-recorded. Earlier: 14 → 15 and 22 → 23 (2026-09-19) for giving way at doorways and shelter spots (the recorded fingerprints happen not to change: no recorded run has a doorway jam or shelters in the side room). Earlier: 13 → 14 and 21 → 22 (2026-09-19) for helping each other. Earlier: 12 → 13 and 20 → 21 (2026-09-19) for closing and locking doors. Earlier: 11 → 12 and 19 → 20 (2026-09-19) for the side room (the recorded fingerprints happen not to change, because no recorded run opens the east door). Earlier: 10 → 11 and 18 → 19 (2026-09-19) for carrying and throwing. Earlier: 9 → 10 and 17 → 18 (2026-09-19) for things catching fire. Earlier: 8 → 9 and 16 → 17 (2026-09-19) for tables and chairs. Earlier: 7 → 8 and 15 → 16 (2026-09-19) for burning people: fire contact sets people alight instead of making them lost, and flames spread between people. Earlier: 6 → 7 and 14 → 15 (2026-09-19) for hard knocks (knock-outs, door damage and breaking, stronger people forcing doors more often) and a fix: trait draws, pace jitter, shout and panic-decision timing now advance the run's random generator instead of a copy of it, so those numbers are no longer tied to the next draw. Earlier: 5 → 6 and 13 → 14 (2026-09-19) for personalities: pace, reaction delay, danger distance, shouting, panic decisions, swerves, hesitation, trips, dodging, ramming, knockdowns and box shoves now depend on traits, and the temperament deck is dealt by fearfulness. Earlier: 4 → 5 and 12 → 13 (2026-09-19; previously 3 → 4 and 11 → 12 for doors and boxes, 2 → 3 and 10 → 11 for temperaments, hearing and collisions) | A body is now moved once per tick. Before, a runner who stopped trying a door had their body moved twice in that tick, so they could turn up to twice their turn rate (24–28° instead of at most 16°, seen with seeds 40 and 46). Events also gained a target ID. Both change replay data, which the [compatibility policy](simulation-contract.md#simulation-compatibility-policy) requires to be versioned. | Any further change to a replay-relevant rule. |
| Code structure (2026-09-19) | The simulation is split into parts that each own their own data. The world geometry (`WorldGeometry`) is the only code that knows the room is a rectangle with doors in its walls. There are separate systems for fire, doors, sound, fear, perception, falls, collisions and boxes, and separate calm, panic and door behaviours. Behaviours only say what the body should do (`MotorIntent`); `Locomotion` is the only code that turns and moves bodies. Each person's record is split into body, personality, fear, intent, hearing and door memory. `FireReactionSimulation` just builds these parts and runs the tick schedule. | Before, one class in nine files shared about 45 fields per person that any file could change. That is how the double-turn bug above happened, and every new stone would have had to touch that shared state. Geometry spread across every file would have made inner walls or a second room a rewrite of most files. | A stone needs inner walls, several rooms or routes: change `WorldGeometry` first. |
| Scenario values (2026-09-19) | Every tunable number lives once, with its default and unit, in a settings group in `ScenarioSettings.cs` (world, perception, fire, steering, calm, panic, temperament, hearing, falls, exits, object physics). The scenario asset stores one block of these groups, and a run works on its own copy. About 40 numbers that were fixed in the code, such as follow strength, door bonuses, stroll distances and steering weights, are now editable in the asset with unchanged values. Only pure mechanics stay fixed in code: sub-millimetre box units, the box contact search steps, and the 75° sharp-turn rule. IDs are called `SimulationId`, because Unity 6.3 already has a type named `EntityId`. | Adding one value used to mean editing four places. Now it is one line. The owner can tune every behaviour number in the Inspector. | Designers need per-scenario presets or curves instead of single numbers. |
| Replay fingerprint (2026-09-19) | `ReplayFingerprintEditModeTests` runs the default scenario for 60 s with seeds 42, 40 and 46, both with every door locked and with two doors opened. It also runs two cases with every box kicked toward the middle. Each run is squashed into one number: every person's position and state on every tick, every event, the final boxes and the random generator's state. A restructure must keep every number. A deliberate behaviour change re-records them and bumps the compatibility version. | This proves a code change is invisible to players, which the owner cannot check by eye. The step-by-step restructure above kept all fingerprints identical. The box cases were added because the default runs contain few box-on-box hits. | A new stone changes behaviour on purpose; re-record in the same commit. |
| Fire queries and snapshots (2026-09-19) | Questions such as "nearest fire", "fire within X" and "can I see fire" only look at grid cells within reach. When fewer cells are burning than it would have to look at, the check falls back to testing every burning cell. Answers, ties included, match checking every cell; a test compares them at 1,681 points for small, medium and large fires. Snapshots copy people, doors and boxes. They only hold a view of the event log and the burning cells, which never shrink. | Before, one panic decision checked about 25,000 cells once the room was alight, and every tick copied the whole event history. Both costs grew with fire size and run length. | Profiling shows people-to-people checks are the cost, for example with many more people; then add a grid for people too. |
| Display structure (2026-09-19) | The display is split into room and doors, people, boxes, fire, ripples, on-screen text and door-click input, all under one "Fire reaction presentation" object. Events name what they affected (`TargetId`), so a kicked box and a shoved door are found by ID instead of "whichever is nearest". Colours use one shared `MaterialPropertyBlock`, and every material is destroyed when the display goes away. The runner creates its run the first time something asks for it, so the display does not depend on Unity's start-up order. | Guessing the nearest box could make the wrong one hop, and materials leaked on every scene reload. | Icons or effects need authored art or pooling. |
| Doors (2026-09-18) | One 1.0 m door in every wall, set 2.5 m off-centre in a pinwheel (north at x = −2.5 m, east at z = +2.5 m, south at x = +2.5 m, west at z = −2.5 m). All start locked. | The owner asked for a door in every wall. 1 m is an ordinary single door: one person at a time, so queues and doorway crushes appear by themselves. Off-centre doors give each corner of the room a different nearest exit. Chosen on the owner's behalf. | Playtests want wider exits, double doors or authored door layouts per scenario. |
| Door clicks (2026-09-18; revised 2026-09-19) | Locked → click → unlocked (green) → click → open → click → closed (unlocked), unless someone is in the doorway. Before 2026-09-19 a click on an open door did nothing; the owner chose to let the player close doors. Clicks are `ClickDoor` player commands for the next tick. Unity's Input System reads the mouse; a ray-cast against a click-only collider on each door leaf finds which door was clicked. | The owner specified unlock-then-open. Closing a door on someone standing in the doorway needs rules the prototype does not have yet, so open is final for now. The Input System is already installed and set as the only input backend; the collider only identifies the door and never decides anything in the simulation. [Input System mouse](https://docs.unity3d.com/Packages/com.unity.inputsystem@1.16/manual/Mouse.html), [Physics.Raycast](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Physics.Raycast.html) | The player needs to close or re-lock doors, or clicks need to work on touch screens. |
| Escaping (2026-09-18) | An open door adds a 1 m-wide walkable strip from 1 m inside the wall to 2 m outside. Only a person heading for that door (or already outside) may use it. 0.8 m past the wall they are `Escaped`, leave the room and shrink out of view. | "Exit" should mean leaving; the short strip lets the player see them walk out. Calm people keep treating doors as wall, since they are not trying to leave. Chosen on the owner's behalf. | Scenarios need rooms beyond the door, or calm people who leave on their own. |
| Door behaviour (2026-09-18) | Runners pick a door by distance, with a 5 m penalty for a route past the fire, an 8 m penalty for a door in the fire, a 4 m bonus if it is visibly open, a 1.5 m bonus for sticking with their current choice, and up to 1.5 m of random noise. At a closed door: 0.4 s to open an unlocked one; 0.5 s rattling a locked one, then 60% shove it every 0.4–0.6 s for 1.5–4 s (a thud others hear), 40% give up at once. Giving up means avoiding that door for 6–12 s and glancing toward the next choice for 0.3–0.6 s. Stuck in a crowd on the way to a door for 12 ticks: avoid it for 2–4 s. If every door is avoided, they fall back to the old "run away from the fire" spot choice. Unlocking a door while someone rattles it makes them open it at once. | The owner asked for people who try doors, sometimes try to force them without success, and look for another way. People can see an open door but not a lock, so red and green are for the player only. Measured headless over seeds 40–46 in the first 20 s with every door locked: 16–29 door attempts, 23–63 shoves, 10–15 give-ups, nobody escapes. With every door open: 8 of 10 escape (the 2 who freeze for good are caught), the first after 1–4 s. Chosen on the owner's behalf. | Playtesters find the rattling too long (shorten forcing) or people too stubborn (shorten the avoid time). |
| Physical objects (2026-09-18; replaced 2026-09-21 by "The physics engine" below) | A physical-object system inside the simulation, not Unity physics: whole-number positions (kept to 1/100 mm), velocities, mass, round footprints the width of the object, spin for looks. Each tick after collisions, every moving object slides, stops at the first person, object or wall in its way, bounces (30% off people and walls, 40% off other objects), loses 1.57 mm/tick of speed per tick to friction (cardboard on concrete, about 0.4), and never goes faster than the 120 mm/tick step limit. Boxes are the only kind: 8 of them, 0.3–0.6 m wide and 3–20 kg. | The owner asked for a foundation for in-world physics objects, starting with boxes. A box can trip a person, so boxes decide outcomes, and the simulation contract forbids Unity physics or floating-point from deciding outcomes. Round footprints are the usual first step. Chosen on the owner's behalf. | Boxes look wrong because corners do not collide, objects need to stack or be carried, or many more objects need profiling. |
| People and boxes (2026-09-18) | A person running into a box pushes it along the line between them as a 70 kg body would. A walker side-steps first and only pushes when boxed in; a runner at 2.5 m/s or more hits it without dodging. At 3 m/s or more there is a chance to trip over it instead: (speed − 3 m/s + 0.5 m/s) × box width ÷ 500, capped at 75%, so about 50% for a 0.6 m box at 4.5 m/s and about 25% for a 0.3 m box. A sliding box into a person: momentum ≥ 800 kg·mm/tick staggers them, ≥ 1,600 trips them; below that it just bounces off. Calm people steer well clear of boxes; runners only a little. | The owner asked for boxes that bump around and sometimes trip people, depending on size and speed. Measured headless over seeds 40–46 in the first 20 s with doors locked: 5–24 box bumps, 0–5 box trips, 0–2 people hit by flying boxes. Chosen on the owner's behalf. | Box trips feel too common or too rare in play; change the 500 scale or the 75% cap first. |
| Personalities (2026-09-19) | Six traits per person, 0–10, with 5 an ordinary person who behaves exactly as the other settings say. The default ten are an authored cast (brute, hero, saint, villain, nervous wreck, sprinter, bully, coward and two ordinary people); people without authored traits get the rounded average of two seeded 0–10 draws, so 5 is common and 0 or 10 rare. Effects, per point away from 5: strength ±10% box-shoving weight, and 4+ points stronger only staggers in a knock-down collision; bravery −10% reaction delay and −5% danger distance; nervousness −10% shout and panic-decision intervals, +5 points swerve chance, +2 points hesitation chance, +10% trip chance; compassion +15% and evil −15% people-avoidance while running, and compassion +5 and evil −5 mm/tick on the speed at which a runner rams rather than dodges (never below 20). Freezing temperaments go to the most fearful (nervousness minus bravery), seed breaking ties. Shown as a number beside each head and a Tab table. | The owner asked for traits that strongly shape behaviour and, later, can be changed by the player. An authored cast guarantees every personality appears in every run, the lesson of the temperament deck. "5 = unchanged" keeps every earlier tuning valid. The owner chose numbers on screen plus a toggled table to keep play clean. Measured headless, seeds 40–46, first 20 s, doors locked: 9–31 collisions, 1–4 knockdowns (4–12 before: strong people shrug hits off and compassionate people dodge), 1–7 trips; all doors open: 6–8 of 10 escape. Chosen on the owner's behalf except the display. | Traits feel invisible in play (raise the per-point percentages), or knockdowns feel too rare for later stones that build on them (lower `StrengthShrugOffGap` effect or knockdown speed). |
| Hard knocks (2026-09-19) | **Knock-outs:** each knock-down rolls 10% + 1% per mm/tick above the knock-down speed − 3% per strength point above 5, capped 0–60%; heavy box hits that trip roll the same with 1% per 100 kg·mm/tick above the trip momentum. Out cold for 6–12 s, then 1 s getting up. **Doors:** strength 7+ damages a locked door by (strength − 6) per shove; damage stays on the door; at 40 it breaks for good. Forcing rather than giving up is +5% per strength point. Measured headless, seeds 40–46, doors locked, first 60 s: 0–10 knock-downs and 0–2 knock-outs per run (knock-outs in 6 of 7 runs); a door broken in 3 of 7 runs (including the default seed 42), 2 of them within the first 20 s. | The owner asked for passing out after hard knocks and for very strong people to break locked doors. A first version gave each shove a 6% × (strength − 6) chance: doors broke in 6 of 7 runs within 20 s and up to 8 of 10 people escaped a "locked" room, which made the player's door clicks nearly pointless. Accumulated damage makes breaking a visible, predictable build-up ("the brute has been battering the north door for 5 s") instead of a hidden roll, which fits "readable emergence". Chosen on the owner's behalf. | Breaking feels too rare or too common (change door strength 40), or knock-outs too rare to matter once people can drag the unconscious (raise the 10%). |
| Burning people (2026-09-19) | Touching fire sets a person alight for a seeded 3–6 s before they are lost. They sprint at their own panic pace, pick a random direction every 0.2–0.5 s (or after 0.1 s blocked), steer off walls but not people, ignore doors, cannot escape, and scream every 0.5–1 s (a normal yell). Flames jump to anyone within 10 cm with 20% per tick, and always to someone in a collision with a burning person. Burning people do not set the floor alight. Measured headless, seeds 40–46, doors locked, first 60 s: 53 people caught fire, 21 of them from another burning person; with every door opened at 5 s, 6–8 of 10 still escape in the first 20 s, as before. | The owner asked that people touched by fire run around for a while instead of dying at once, spreading the fire by running into others. 3–6 s is long enough to watch and short enough that the room does not fill with burning people. Refusing escape keeps "a door saved them" and "the fire got them" from contradicting each other. Chosen on the owner's behalf. | Burning people feel too deadly to the crowd (lower the 20% or the 10 cm) or too harmless (raise them), or the owner wants burning people who can be saved (a later extinguisher). |
| Tables and chairs (2026-09-19) | Three 1.2 × 0.7 m tables as fixed rectangles owned by `WorldGeometry`, with square-cornered collision (the rectangle grown by the body's radius). Bodies slide along table edges; objects bounce off them like walls; tables push people away like walls; random spots are redrawn up to 8 times to stay 0.3 m clear; routes into a table score 3 m worse. Eight 0.45 m, 5 kg chairs are ordinary loose objects of a new kind. Measured headless, seeds 40–46, doors locked, first 20 s: 5–20 collisions, 1–7 trips, 7–24 kicks of boxes and chairs; all doors opened: 6–8 of 10 escape, as before. | The owner asked for tables and chairs. Rectangles with local steering are enough for three tables in open floor; square corners are a quarter-body-width approximation that keeps the maths exact and simple. Chairs reuse the box physics so they can be kicked and (next) picked up. Chosen on the owner's behalf. | People visibly snag on table corners or get trapped between furniture; then round the corners or consider AI Navigation. |
| Things catch fire (2026-09-19) | Heat instead of instant ignition: a thing within 0.5 m of flames gains a tick of heat each tick and catches at 1.5 s (cardboard box), 3 s (chair) or 5 s (table); heat never cools. Burn times: boxes 8–15 s, chairs 12–18 s, tables 20–30 s, then charred and inert. A burning thing lights the floor square it has rested in for 1 s (so a sliding box lights nothing until it stops) and sets alight anyone within 5 cm; a burning person sets alight anything within 5 cm. Burning things light floor squares only through that rule, so vision, danger and contact rules still only look at squares. Measured headless, seeds 40–46: 7–17 of the 19 things alight in the first 20 s (15 s of fire); every one burns within 60 s; things lit 93 floor squares across the seven minutes; with every door opened at 5 s, 5–8 of 10 people still escape. | The owner asked for objects that catch fire when close to fire. Heat over time reads as "it started smoking, then went up", and different times for cardboard and wood make the room's fuel matter. Lighting the floor only where a thing rests makes a kicked burning box a readable way fire jumps across the room, instead of a trail of fire along its path. Chosen on the owner's behalf. | Fire races too fast now (lengthen ignite times or the 1 s rest), or burning things are too rarely seen before the room is ablaze. |
| Carrying (2026-09-19) | Items are the existing physical objects with a holder. Lift limit 5 kg + 2.5 kg per strength point; carrying slows by up to 40% in proportion to the load's share of that limit. Calm tidying on 12% of fresh decisions (nearest liftable item within 4 m, carried at least 1.5 m, pick-up 0.5 s, set-down 0.4 s). Startled carriers: nervousness 6+ drop, others throw; down or burning, drop. Runners with strength 6+ hurl items in their way; evil 7+ aim at the nearest person within 4 m. Throw speed 60 × (strength + 5) ÷ (kg + 5) mm/tick; thrown hits count ×3 momentum. Held items take no part in collisions. Calm tidying is unlogged, like calm pushes. Kinds carry no special abilities yet; a fire extinguisher would be a new kind with a "use" action. Measured headless, seeds 40–46, doors locked, first 20 s: 2–11 items thrown per run, about one hard hit per run; all doors opened: 4–8 of 10 escape. | The owner asked that people can pick up, set down and throw boxes, as a base for items like fire extinguishers. Reusing the physics objects keeps one kind of thing on the floor. The ×3 hit rule exists because a sliding 5 kg chair can never hurt anyone under the box rules, and a thrown one should. Chosen on the owner's behalf. | Tidying looks too busy or too rare before the fire (change 12%), or thrown items hurt too often (lower ×3). |
| Side room (2026-09-19) | A 2 × 2 m room behind the east door (x 6–8 m, z 1.5–3.5 m) as a second walkable rectangle in `WorldGeometry`, not a general room graph. The fire grid grows to cover it; cells take the room of their centre; fire crosses rooms only through the open door gap. Sight and touch ignore fire in a room behind a shut door; noises between such rooms carry half as far. Runners pick the door like any exit and shelter inside at the back wall; with fire inside they leave. Measured headless, seeds 40–46, every door opened at 5 s, first 20 s: 4–8 of 10 escape through the three outside doors. | The owner asked for a small room for 2–3 people behind one door, and for fire that does not pass closed doors. One extra rectangle keeps the walking rules as simple as the doorway strips already are. Half reach through a door is the simplest "muffled" that still lets people inside hear a commotion. Chosen on the owner's behalf. | Scenarios need several rooms, rooms behind rooms or inner walls; then replace the rectangles with a room-and-door graph. |
| Closing doors (2026-09-19) | The player closes an open door with a click; nobody may close a door on a body in its doorway. People decide at two moments: on escaping (the door behind them) and each tick while sheltering within 2 m of their side room's door. Rules in order: evil 7+ shut and lock; fire within 2 m of a shelter's door, shut; someone else within 3 m, leave open; compassion 7+ with fire over 5 m away, leave open; nervousness 8+, shut; bravery + compassion 12+ with fire within 5 m, shut. Only the evil lock. No randomness. Measured headless, seeds 40–46, every door opened at 5 s, first 20 s: the two evil people lock a door behind them in 6 of 7 runs; 4–7 of 10 escape. | The owner asked for people who close doors to stop fire spreading, closing behind them by personality, and evil people who lock a door behind them; and chose that the player may close doors too. Deciding at fixed moments with fixed rules keeps every slam explainable from the Tab table. Chosen on the owner's behalf. | Evil locking feels too common (raise 7) or the side room door is closed too eagerly or never. |
| Helping each other (2026-09-19) | Shaking: compassion 6+, bravery 4+, evil 4 or less, target frozen within 4 m, 1–1.5 s of shaking; frozen-for-good wake with 50%. Dragging: strength 6+ and compassion 6+, evil 4 or less, target knocked out within 5 m, 1 s to grab, pace 10 + 2 × strength mm/tick, toward the nearest open outside door (else 3 m from the fire); the helper's escape rescues the dragged person. Helpers abandon in danger, when down, burning, stuck 2 s or unable to reach in 5 s; nobody targets someone already near the fire. Measured headless, seeds 40–46: 1–3 people shaken awake in most runs within 20 s; a drag in 1 of 7 one-minute runs with doors locked (only the hero qualifies); with every door opened at 5 s, 2–8 of 10 escape (helpers who turn back sometimes do not make it). | The owner asked for compassionate people who shake the paralysed awake and strong, compassionate people who drag the knocked-out to safety. Fixed trait thresholds make every rescue explainable from the Tab table; a helper's escape counting for the dragged person makes a drag a clear rescue. Chosen on the owner's behalf. | Drags too rare to see (lower the strength threshold to 5) or heroes die too often (skip targets nearer the fire). |
| Leadership (2026-09-20) | A seventh trait, leadership, drawn like the rest. Leaders (7+) form one plan every 1–2 s: send a strong person at a door they have found shut, send a brave one for an extinguisher, or call the people near them on. Orders are events naming the person told, and set that person's own intent — never a hold over them; an order lasts 15 s, a following 8 s. Obedience is 55% + 4% per nervousness − 3% per bravery, refused outright by evil 7+ or by anyone who leads as much as the leader. The frozen hear nothing. Measured headless, seeds 1–8 with every door locked: leaders call people on in all eight, send someone at a door in four, and in one of those the door is broken down and nine people get out. | The owner asked for leadership that produces plans like "break down that door, you do it" and gathers people. Building it out of the pieces already here (the door-forcing, the extinguisher, the escape routes) keeps it emergent rather than scripted: the leader only redirects what someone would already be able to do, and the order can be refused. Chosen on the owner's behalf. | Leaders should plan together, pass orders on, or be disbelieved once their plan fails; then orders need a standing state per person rather than one nudge. |
| Slammed doors are a villain's move (2026-09-21) | Shutting the door behind you as you leave a room or the building is now **evil 7+ only**, and turning the key as well is **evil 9+**. Shutting a door with fire in the room beyond it is a separate act and open to anybody, kind or cruel, because that is keeping flames out rather than people. The kind (compassion 7+) still hold such a door for somebody coming, unless the flames are within 2 m of it. The nervousness 8+ and bravery-plus-compassion 12+ rules are gone, and so are their settings. **Measured headless, seeds 40–46, doors locked, first 20 s:** about one door is shut per run and a key is turned in roughly one run in three, against a door shut in nearly every run before. | **The owner asked for** only evil agents to close doors behind them when escaping, with the really evil also locking them. Splitting the one tangled rule into two — spite, and fire containment — is what makes that possible without making the side room a death trap: somebody sheltering in a cupboard still shuts the door on an approaching fire. **Chosen on the owner's behalf:** the locking bar of 9, so that "really evil" is a step past "evil" rather than the same thing. | Slammed doors become so rare that the mechanic stops reading, or evil 9 never turns up in a drawn cast; then lower the locking bar to 8. |
| Shoving people aside (2026-09-21) | Running into somebody is an accident that needs speed; a shove is deliberate, needs none, and only **evil 7+** do it. When a cruel runner's straight step is blocked by somebody and the closing speed is too low to count as a collision, they take hold and heave instead: the person shoved is sent **350 mm** clear, and goes down rather than merely reeling if the shover is **2 or more** strength points stronger. One shove per person per **0.8 s**. It makes a thud, and a calm person shoved is alarmed. Somebody frozen with fear can be heaved aside too — they never move of their own accord, but this is somebody else's doing. **Measured headless, seeds 40–46, doors locked, first 20 s:** about four shoves a run, which is often enough to notice in a doorway queue and rare enough not to be constant. | **The owner asked for** cruel people who knock others out of the way running for cover. Hanging it on the *slow* case is what makes it visible: collisions already covered fast runners, so the gap was exactly the queue at a doorway, where nothing used to happen. **Chosen on the owner's behalf:** the 350 mm push, the 0.8 s pause and the 2-point strength gap. | Shoves look like teleporting rather than heaving (shorten the push), or a bully clears a whole doorway too easily (lengthen the pause). |
| Bags and briefcases (2026-09-21) | A person can be authored holding something: a new `CarriedObjectId` on the agent definition, bound to them at tick zero. Four of the twenty now walk in with something — the brute and the other bully with briefcases, the saint and the worrier with bags. Their own possession is marked as theirs, so they keep hold of it while calm instead of tidying it away, and let go only when something frightens them. The throw itself is now a reflex rather than a placement: the direction veers up to **±60°** from wherever they happen to be facing, except that the cruel still aim at the nearest person. **Measured headless, seeds 40–46, doors locked, first 20 s:** about fifteen things thrown and five dropped per run, up from a handful, and thrown things hit somebody noticeably more often. | **The owner asked for** agents throwing things in their possession when scared enough, as a primal reaction that does not have to make sense. Most of the machinery was already here (carrying, throwing, the cruel aiming at people); what was missing was anybody *starting* with something and any wildness in the direction. **Chosen on the owner's behalf:** the ±60° spread, and which four of the cast carry something. | Everybody throwing at once reads as farce rather than fright; then gate the reflex on nervousness instead of on being frightened at all. |
| Fire alarms (2026-09-21) | A red box on the wall of each room people use. A **scared** person who is not in immediate danger, whose hands are free, and who is either a leader (6+) or thinks of others (compassion 6+) breaks off to hit the one in their own room: **4 m** to divert for, **0.4 s** to press. Hitting one rings **every** bell in the building, each as its own noise in its own room, so a shut door cannot leave a room uninformed. A bell tells somebody there is a fire without showing them one, and that is the whole point of it: somebody whose **bravery less nervousness is 2 or more** keeps their head — they still head for a way out, but at walking pace, never swerving, never dithering and never freezing. Everybody else panics as they always did. Composure ends the moment the fire stops being an abstraction: it comes within their danger distance, they are knocked down, shoved, set alight, or they see the flames themselves. `Alarm.Enabled` turns the whole thing off. **Measured headless, seeds 40–46, doors locked, first 20 s:** an alarm is raised in about six runs in seven, always by somebody kind or a leader, and all three bells ring. | **The owner asked for** agents who can trigger alarms that put the building in panic mode, with a settings toggle because it might be overpowered, and **chose** that what a bell does should depend on who hears it. That choice is what stops it being a button that always helps or always hurts: a bell in a room of cool heads empties it neatly, and a bell in a room of anxious people starts a stampede. **Chosen on the owner's behalf:** every bar and reach above, and the decision to put composure on a flag consulted in three places rather than adding a fourth fear state, which would have touched every `Scared` check in the codebase. | Composure reads as people ignoring a fire rather than coping with one; then give the composed their own icon and gait rather than just a slower pace. |
| Breaking and popping (2026-09-21) | Two new per-kind numbers rather than a code path per kind. **Breaking:** a hit above a kind's `BreakMomentum` (**450** for a wooden chair, **400** for an office chair, **600** for a table, in kilograms × mm/tick) smashes it. A smashed chair becomes wreckage — three-quarters the size, half the weight, still something to trip over, and nobody can sit on it. A smashed **table stops being an obstacle at all**, so people run straight across ground they had been walking around all game. **Popping:** `Microwave` and `WallSocket`, which go off the moment the flames reach them: a bang heard six times the blast radius away, every loose thing within **2.2 m** (microwave) flung away at **3.5 m/s**, anybody within it knocked off their feet, up to **3** floor squares set alight, and the thing itself left as wreckage. **Measured headless, seeds 40–46, doors locked, first 20 s:** something breaks in about half the runs and something pops in about one in seven; over a full minute pops are routine. | **The owner asked for** breakable chairs and tables, and electrical things that pop and might set more things on fire, with stuff flying. Keeping it in the per-kind table is what the earlier "office things per kind" row named as the trigger for exactly this work. A smashed table changing the shape of the room is the most player-visible thing in the batch. **Chosen on the owner's behalf:** every number above. The first thresholds were set at 2,400 and nothing ever broke, because a hurled thing only carries 200–700 on that scale; they were measured and lowered. | Furniture breaks so readily that the room is wreckage within a minute, or a pop chains into every other appliance at once. |
| Wedged doorways (2026-09-21) | Anything resting within its own radius plus **150 mm** of a wall line, in front of a gap, jams that door — from either side, and both ways: it can neither be opened nor shut. Worked out once at the end of each tick, so decisions read a settled answer; a door jammed this tick is noticed from the next, which is 20 ms of staleness in exchange for an O(1) check inside a per-person, per-door loop. People discover a jammed door exactly as they discover a locked one, and somebody **strong (7+)** heaves the obstruction out along the wall instead of giving up. And people wedge doors **on purpose**: the frightened (nervousness 7+) to keep the fire out, the cruel (evil 7+) to keep other people out, never the kind while somebody is still coming through, and never their own way out unless the room beyond it is already alight. **Measured headless, seeds 40–46, doors locked, first 20 s:** about two doorways jam per run by accident, one obstruction is heaved clear, and a door is wedged on purpose in about three runs in five. | **The owner asked for** objects that block doors from opening and closing, agents who barricade doors for safety, and evil agents who block them for selfish reasons. Defining a blockage by proximity rather than by letting objects travel through doorways is what kept this cheap: object movement is untouched, so the feature added no new physics and no new determinism risk. **Chosen on the owner's behalf:** every bar and distance above, and making the barricade timings fixed rather than seeded so this feature adds no randomness of its own. | Boxes should roll through open doors between rooms; then objects need to leave their room and `KeepObjectInRoom` needs a doorway exception, with the wall-line bug in its fallback fixed first. |
| Influence and cards (2026-09-21) | The player starts with **100** influence, each card spends some (Beefcake **20**, start a fire **10**, put down an extinguisher **25**, TNT **40**), and the only thing that pays any back is a person getting out alive, at **15** each, capped at **300**. A card that cannot be paid for, or cannot be played where it points, does nothing and costs nothing. Cards are picked with **1–4** and played with a click: on a person, or on a place. The queue that used to live inside the door system is now its own `PlayerCommandSystem`, and a command can name a place (whole millimetres) as well as a thing — screen positions and rays never leave the presentation. Influence itself is not in the causal log, for the same reason sitting is not; each card's own event records what it cost. | **The owner asked for** an influence meter that starts at a set amount, depletes per action and rises per agent saved, and **chose** that saving people should be the only thing that refills it. That choice is what gives the meter teeth: spend it all and save nobody and there is nothing left to do but watch, and a good run compounds because twenty people saved funds seven more cards. **Chosen on the owner's behalf:** every cost and the starting amount, which are first guesses meant to be retuned after a playtest; they all sit in one settings group. | The first playtest shows the opening hand is too generous or too mean, which is a matter of changing five numbers. |
| TNT (2026-09-21) | A blast hole is one of **four** spare openings the scenario reserves, kept out of the world until a charge is spent, then filled in as a **`Broken` door 1.5 m wide**. That is the whole trick: `Broken` already means permanently open, unclickable, unshuttable, unlockable and unbatterable, and already counts for walking, routing, fire spread, sound and escaping — so a hole needed no new rules in any of those. The player clicks within **0.9 m** of a wall; the hole must fit wholly in one wall line with a body's width of wall at each end, must keep **0.5 m** clear of any other opening in the same line, and must not open half into the next room and half into solid wall. The bang carries **20 m** (frightening within 12 m), flings loose things within **2.5 m** at 4.5 m/s, and floors anybody within **1.5 m**. It starts no fire. **Measured:** a hole in an outside wall becomes a way out and an escape through it names the blast as its cause; a hole in a shared wall lets fire cross. | **The owner asked for** TNT that blasts a hole in a wall. Reusing a door slot rather than inventing a wall model is what made this affordable at all: there are no walls in the simulation, only the edges of room rectangles, and every opening is a door. **Chosen on the owner's behalf:** every number above, and that TNT starts no fire, so the card stays a rescue tool rather than a mixed blessing. **A limitation worth knowing:** no interior wall in the current floor plan is long enough to take a 1.5 m hole clear of the door already in it, so in this building TNT makes exterior holes only. | A floor plan with longer interior walls arrives, or the hole should be narrower so interior breaches become possible; that is one number. |
| Spare slots for things made mid-run (2026-09-21) | Anything the player creates during a run — a blast hole, an extinguisher put down — comes from a fixed pool the scenario authors, not from growing an array. A spare is not in the world: nothing can touch it, reach it, burn it, sit on it or see it, and it is not drawn. | Every per-entity array in the run is sized once at construction — each person carries one entry per door, for instance — so creating something mid-run would mean reaching into twenty people and inventing initial values. A fixed pool keeps every array length and every index stable for the whole run, which is what replay depends on. The trap this avoids is precise and was caught by a test: an unplaced slot left in the loops reads as *a door leading outside*, so people would score it, draw a random number for it, and silently change every recorded run. There is a test that an unused pool leaves the fingerprints and the generator's final state untouched. **Chosen on the owner's behalf.** | Something needs to be created in numbers that cannot be reserved up front, such as debris per broken thing; then the per-entity arrays need to grow together in one place. |
| Fire extinguishers (2026-09-20) | Fire can now go out: a burning square takes 0.6 s of spray to douse and then stays wet for 20 s, so it cannot simply re-light behind the person fighting it. A bottle holds 6 s of spray, one square at a time, which beats a fire caught in its first seconds and is hopeless against a grown one. Measured headless, seeds 1–12: someone tries in 10 runs, the fire is beaten outright in 1, the bottle is emptied in vain in 3. The jet also floors whoever is standing in it (400 mm back, once per 2 s) and puts out burning people and things. Recoil is 60 mm a tick less 12 mm per strength point, so strength 5+ is steady; strength 0 is floored by it. | The owner asked for an extinguisher with very limited fuel, used on the fire or on burning people, with weak people blasted back. The value in play is a gamble: taking it on early can save the office, taking it on late wastes the bottle and the seconds. Wetting squares was needed because otherwise the fire immediately reclaimed each square and no bottle could ever achieve anything. Chosen on the owner's behalf. | Playtests show the fire is beaten too often (shorten the fuel or lengthen the douse), or people should hand the bottle to each other. |
| Sitting on chairs (2026-09-20) | A calm person's next-thing-to-do roll picks sitting 14% of the time (the band just above tidying): they walk to the nearest free chair in the room within 6 m, settle onto it when they are beside it, and stay 5–20 s. Settling puts their body on the seat, and standing steps them clear again — the only two places a body moves outside the movement phase, each by less than a stride. An occupied chair is pinned: it does not slide, cannot be lifted and cannot be tidied. Getting out of a chair costs 0.8 s less 0.04 s per nervousness point. Sitting logs no events. | The owner asked whether people could sit on chairs. The value in play is the moment the fire starts: some people are in chairs and lose a beat getting out, which spreads the crowd's reactions apart. Pinning the chair avoids a person being carried around on a sliding chair, which no rule here could make look right. No events, because sitting causes nothing else in the world and the causal log's rule is that every event but the first has a cause. Chosen on the owner's behalf. | Chairs need to be dragged or fallen out of as physical objects, or someone should be able to be tipped out of one. |
| Office things per kind (2026-09-20) | Each kind of loose object has an entry in the scenario's table of kinds: friction as a percentage of the floor's (35 for an office chair on castors, 55 for a laptop, 200 for a potted plant), and how it burns, where ignite time 0 means it never catches. Added: office chairs, waste bins, potted plants, bags and laptops, spread across both big rooms, plus the meeting room's long table in two halves. | The owner asked for more random items that interact physically, in an office. One table keyed by kind keeps the numbers in one readable place instead of a pair of fields per kind, and gives each thing a distinct feel when kicked: the chair rolls away, the laptop skitters, the plant barely shifts. A thing that never burns needed a rule of its own, so 0 means never. Chosen on the owner's behalf. | A kind needs behaviour beyond sliding and burning (rolling downhill, shattering, spilling); then each kind needs its own code path rather than a row of numbers. |
| Seeing through walls (2026-09-20) | A 40-line shader, `Content/Rendering/SeeThrough.shader`: unlit, transparent, queued after everything, `ZTest Greater` and `ZWrite Off`, so it paints only where something nearer the camera already has. The presentation adds it as a **second material** on each person's and each object's renderer, which makes Unity draw that mesh a second time with it. Nothing else is touched: no layers, no renderer features, no project settings beyond keeping the shader in every build. | The owner asked to see people and items through walls. The first attempt followed Unity's [Render Objects recipe](https://docs.unity3d.com/6000.4/Documentation/Manual/urp/renderer-features/how-to-custom-effect-render-objects.html), which needs a layer left out of the opaque mask and two passes added to the renderer asset — and it silently did nothing, because it depended on a setup command the owner had already run once and which returned early the second time. A rule that the game cannot draw itself without a menu command having been run is a rule that will keep breaking. URP's own shaders expose no depth-test control, so the second material needs a shader of its own; forty lines of it buys a feature that works the moment the scene runs. Chosen on the owner's behalf. | Twenty silhouettes look cluttered, or the look wants depth cues a flat colour cannot give; then the same shader grows a fresnel edge or an outline. |
| The office floor plan (2026-09-20) | The building is an open-plan office 12 × 12 m (x −6 to 6, z −6 to 6, the fire starts here), a 2 × 2 m storage closet on its east wall (z 1.5 to 3.5), a 3 × 2 m corridor east out of the office at z 0, and a 12 × 12 m meeting room beyond it (x 9 to 21). Ways out: north, south and west from the office, north and east from the meeting room, all locked at the start. Ten people in each big room. The camera frames every room automatically rather than a fixed size. | The owner asked for a second room of the same size joined by doors and a short corridor, an office setting, and ten more people in the new room. A corridor at the office's mid-height keeps the closet where it was and gives the two crowds a single-file pinch point to meet in. Twenty people is the profiling checkpoint's trigger, so a standalone build is recorded with this stone. Chosen on the owner's behalf. | Revised 2026-09-21: see the floor-plan row below for the smaller meeting room, its single way out, and the wider corridor. |
| Rooms as a graph (2026-09-19) | `WorldGeometry` now holds a list of non-overlapping rectangular rooms; a door belongs to one room's wall and joins it to whichever room lies flush beyond, or to the outside. Walking, steering off walls, doorway strips, fire spread, sight, sound and escaping all ask "which room", not "the main room". Routes between rooms come from a Dijkstra search over the rooms (a dozen at most, so a plain scan for the nearest unsettled room), costed door centre to door centre, reusing one set of scratch arrays so a run allocates nothing. Sheltering is gone: a runner scores the ways out of the building by route length and runs for the first door on the way; a door they have stood at and failed to open stops counting; with none left they score rooms by distance from the flames, skipping rooms already holding one person per square metre of floor. Inside doors start shut but unlocked. | The owner asked for the closet to stop being a special safe room, for people to save themselves wherever they can, and for a second room joined by a corridor. The side-room rectangle could not express a corridor between two rooms, and the "Side room" row below named exactly this as the trigger to replace it. Rooms-as-a-graph is the smallest model that covers both, and keeps every spatial question in one class. Chosen on the owner's behalf. | A floor plan needs non-rectangular rooms, rooms inside rooms, or stairs; then rooms need a polygon or grid model, and routing needs waypoints inside rooms rather than door-to-door distances. |
| Doorway jams (2026-09-19) | A runner blocked for 12 ticks within 0.85 m of their open door, inside the room and not lined up with the gap, gives way: for 25–50 ticks they head for a spot 0.4 m inside the wall, 0.85 m along it from the door's centre on their side, then try again. People sheltering in a side room take the first free of five spots (back middle, back corners, front corners beside the door); a spot is taken when someone else nearer to it stands within 0.3 m. Measured headless, seeds 1–30, every door unlocked at 6, 8 or 12 s, 60 s each (90 runs): the longest anyone stood still next to an open door fell from 1,407 ticks (28 s) to 19 ticks, jams over 2 s fell from 9 runs to 0, and escaped plus sheltering people rose from 687 to 724. | Owner report, seed 5: with the side room's door unlocked, person 9 blocked it. Two runners reached the 1 m door at once, each ended up against one side of the frame, and the one lined up behind them could not pass; nobody moved until the fire came. A second case: everyone sheltering aimed for the same spot at the back, so the second one in stopped just inside the door and blocked the next. Stepping aside and fixed spots are the smallest rules that clear both, and read as ordinary courtesy in a queue. Chosen on the owner's behalf. | People visibly hover beside doors too long (shorten 25–50 ticks), or larger side rooms need more spots (derive spots from the room size). |
| The office floor plan (revised 2026-09-21) | The meeting room shrinks from 12 × 12 m to 10 × 9 m (x 9 to 19, z −4.5 to 4.5) and keeps one way out of the building, on its east wall at z 2.5; the north door is gone. The corridor widens from 2 m to 3 m (z −1.5 to 1.5). | The owner asked for a meeting room that is smaller than the office and has a single escape door besides the corridor, so leaving it is a choice between the length of the room and going back toward the fire. The corridor had to widen to match: with one exit fewer, most of the meeting room comes back down it, and two people meeting head on beside a doorway in a 2 m corridor wedged for over two seconds (caught by the doorway-jam guard). **Chosen on the owner's behalf:** every dimension above. | Playtesting shows the meeting room feels cramped, or the single exit makes it a death trap rather than a decision. |
| Nobody forces the one way out (2026-09-21, the owner's decision) | Door strength stays at 40 and only somebody of strength 7 or more does any damage at all, which in practice means a locked door is never broken down: the player opening it is the only way out of the building. | The owner was asked directly, having watched a roomful of people hammer on the locked exit to no effect, and chose it: it makes the one job unmissable and the stakes absolute. The futile hammering is the rule working, not a fault. Measured in seed 42, the two people who tried the door delivered four points of damage between them against the forty it needs. | The owner wants a trapped crowd to have a last resort, in which case lowering the door's strength is the dial. |
| Fighting only the fire you are in the room with (2026-09-21) | Somebody will only pick up an extinguisher, and only keep carrying one, for a fire burning in the room they are standing in. With the flames next door they put the bottle down and run instead. | Carrying a bottle somewhere is a straight line at the target, and nothing routes it through a doorway yet, so a fire next door meant walking into the wall between. In seed 42 the meeting room's leader did exactly that for over twenty seconds, sliding along the west wall, and because they had the highest leadership in the room five people followed them into the corner and stayed there — which is what the owner saw as everybody huddling in the west corner. Scoping the behaviour to one room is what the movement can actually do today. | The navigation rebuild lands and somebody can walk to another room; then they can go and fight a fire there. |
| One way out of the building (2026-09-21) | The office loses its north, south and west doors. The building now has exactly one way out, in the meeting room, and it starts locked. Everybody in the office has to cross the corridor and the meeting room to escape. | The owner asked for it: the single exit makes the corridor the pinch point the whole building funnels through, which is the situation the prototype is for. Tests about how a door behaves, rather than about the floor plan, now author their own door instead of borrowing one from the building, so changing the plan again will not break them. | The single route makes runs feel the same every time, or the corridor queue stops being interesting. |
| Stepping sideways along a wall (2026-09-21) | The ways round an obstruction now include a step straight sideways (90° either way) as well as 30° and 60°. | With one way out, everybody funnels through three 1 m doorways, and somebody pressed against a wall beside a doorway with the crowd in front of them had no move that got them out of it: their sideways options stopped at 60°, which still pointed into the wall. They stood still until whatever was in front of them happened to move. Measured over twenty runs of the doorway-jam guard, the longest anybody now stands still is 3.3 s and nobody is left rooted at the end. The guard itself was relaxed from 2 s to 5 s, because with a single exit a queue at a door is expected and the original bug it was written for was somebody stuck until the fire arrived. **Chosen on the owner's behalf:** the extra angles and the 5 s limit. | The navigation rebuild lands and people route round each other properly. |
| A meeting in progress (2026-09-21) | Nine of the meeting room's ten people start sitting, one per chair, at a 5.4 × 1 m table in two touching halves: four down each long side and one at the head, each chair facing the table. Six have a laptop in front of them. The tenth stands at the near end of the table presenting. A chair carries a facing, and anybody who sits turns to it at their usual turning pace rather than snapping round. Somebody who starts seated stays 3000 ticks (60 s) unless something moves them, so the meeting breaks up because of the fire and nothing else, and seating draws no random numbers, so the start-up draw order is untouched. | The owner asked for a meeting already under way when the fire starts. Seating had to be authorable: the sitting behaviour existed but could only ever begin mid-run, on a 14%-per-decision whim. **Chosen on the owner's behalf:** the table size, who sits where, how many laptops, and how long they stay seated. | The meeting reads as static, or the owner wants people arriving and leaving during it. |
| Things that rest on things (2026-09-21) | A physical object may start resting on a table or on another object. While it rests it is not an obstacle, does not slide and is drawn at the height of whatever holds it up; it still burns, heats and can be picked up. Lifting, throwing, shoving or blasting it, or smashing the table under it, brings it loose, and it then lands on the nearest clear floor (searched outward in 50 mm steps up to 1.5 m, preferring the way it was sent). A box stacked on another topples off the moment the lower one is kicked, lifted or smashed, and inherits its cause so anything it goes on to do can be explained. | The owner asked for laptops to be on tables only, and for smaller boxes with some stacked. Without a resting state a laptop on a desk is an obstacle inside a solid rectangle, and validation refuses it outright. The landing search exists because a thing that simply dropped would be left inside the table or overlapping the box below. **Chosen on the owner's behalf:** the search step and reach. | Objects need real height, or something has to rest on something that moves. |
| What things look like (2026-09-21) | Cardboard boxes are 0.25–0.4 m and 2–6 kg, against walls with three pairs stacked, none within reach of a doorway. People are drawn exactly as they always were: a capsule 0.5 m across and 0.5 m from middle to end, the width matching the 250 mm body radius the simulation uses, so what you see is what bumps. | A display bug was overwriting every box's authored size with a 1 m cube each frame, which is why they looked huge. People were briefly drawn at a real adult's height as well; the owner did not want that and it was put back the same day. **Chosen on the owner's behalf:** the box sizes only. | The owner wants a different visual scale for the whole scene. |
| Turning as things slide (2026-09-21) | Spin is derived from the knock that caused it: `SpinMaximum` (8°/tick, about 400°/s) scaled by the speed given and by 200 mm over the object's radius, so a light laptop whips round and a heavy plant barely turns. A knock sets the turn rather than adding to it, so brushing past a box tick after tick cannot wind it up; the turn decays with the same drag that slows the slide, and anything at rest does not turn at all. A thrown thing takes its rate from the throw, with only its direction a coin toss. | Spin was an independent cosmetic counter capped at 20°/tick (1000°/s, nearly three turns a second), added to every tick of contact but decaying only 1°/tick, never cleared on release, bounce or friction, and applied to stationary objects. The owner reported objects spinning unrealistically. **Chosen on the owner's behalf:** the cap and the reference radius. | The physics rebuild gives objects real angular momentum. |
| Seeing through walls (revised 2026-09-21) | Walls and door leaves carry a second, invisible material (`Content/Rendering/WallMark.shader`) that writes the lowest stencil bit wherever they are the nearest thing to the camera. The silhouette pass now tests that bit as well as the depth test, so it only paints where a wall or a door is in the way. Fire cells and flame cubes get a silhouette too, in orange. URP reserves the upper stencil bits for its own lighting and leaves the lower four to projects. | The silhouette painted wherever anything nearer had already drawn, including another part of the same object, so a chair showed its own back legs through its own seat and anything behind a table ghosted across the table top. The owner reported both. Fire was invisible behind a wall, which matters most when somebody sets off toward it with an extinguisher. **Chosen on the owner's behalf:** the stencil bit and the orange. | Twenty silhouettes look cluttered, or the look wants depth cues a flat colour cannot give. |
| Bangs you can see (2026-09-21; sparks and smoke are particles since "Particle effects" below) | An `ObjectExploded` or `PowerBlastedWall` event draws a burst built from primitives: a bright sphere that swells and vanishes in 0.18 s, a point light that fades over 0.35 s, ten sparks thrown outward on arcs that fall to the floor over 0.65 s, three smoke puffs that swell and thin over 1.1 s, and a camera shake that dies away over 0.3 s — all sized from the blast radius the event already carries. The scatter comes from the event ID, never the run's dice. Laptops now pop as well: 0.9 m, fling speed 45, one square lit, smaller than a wall socket's 1.4 m and a microwave's 2.2 m. | The owner reported never seeing pyrotechnics. The simulation flung objects, floored people and scattered fire, and the display answered with one flat ring on the floor — the same ring used for a footstep — and a small hop. Laptops never popped at all: their pop radius was authored as 0. **Chosen on the owner's behalf:** every duration, count and size above. | The effects cost noticeable frame time, or the owner wants a different register of violence. |
| Noticing an extinguisher put down (2026-09-21) | Standing a spare extinguisher down with a card marks everybody in the same room within 6 m: for the next 400 ticks (8 s) they need 3 points less bravery than usual to go and take it. Same room only, so it needs nothing that local steering cannot do. | The owner reported never getting anybody to pick up a placed extinguisher. Measured over 40 seeds, somebody picked up the bottle in 7 runs before and 35 after. **Chosen on the owner's behalf:** the bonus, the duration and the range. | Everybody dives for every bottle, or navigation lands and the same rule can work across rooms. |
| Doors you shut yourself (2026-09-21) | Somebody who closes or locks a door behind them records it in their own memory of doors, exactly as giving up on one does, and stops heading for it. The door filter also tests that memory against the door being walked at now, not only the way out at the end of the route. | The owner reported people locking a door and then hammering on it. The locker wrote nothing into their own memory, so on their next thought the door was a way out again; and the filter read the avoid timer for one door and the found-shut flag for another. Measured over 40 seeds, people barged a door they had locked themselves 64 times before and 0 after, with escapes slightly up. | Never, unless door memory grows. |
| An order beats following (2026-09-21) | Somebody sent at a door, or sent for an extinguisher, stops following their leader at that moment. | Following was checked before orders, so anybody already following kept following and the order was never carried out. Measured over 40 seeds, the door was broken down in 25 of the 26 runs where the order was given. | Never; you cannot both trail after somebody and run off to do as they say. |
| Hauling somebody and getting nowhere (2026-09-21) | A dragger counts their own stuck ticks, and lets go after 40 of them (0.8 s). | The count used to live on the body, where the dragger's own accepted step cleared it every tick before the drag was undone, so it never climbed past 1 and the give-up rule could never fire — the opposite of what its own note promised. The limit allows for letting go as well as holding on: somebody wedged in a corner needs a moment more to steer out once their hands are free. **Chosen on the owner's behalf:** the 40 ticks. | Navigation lands and draggers can route around what blocks them. |
| Running the edit-mode tests outside Unity (2026-09-21) | `tools/RunEditModeTests.ps1` compiles the simulation, the edit-mode tests, four small Unity stand-ins and a reflection-driven runner into one assembly with the editor's own bundled Roslyn compiler, and runs them on the installed .NET runtime. It reproduces all ten replay fingerprints, and the whole suite takes about twenty seconds. The two checks it genuinely cannot make in-process are covered another way rather than dropped: the fixed timestep is read from `ProjectSettings/TimeManager.asset`, and the saved scenario asset is compared with the code defaults by parsing its YAML. Anything still unchecked is printed as skipped at the end of every run, never as passed. | Unity's batch-mode runner cannot open the project while the editor has it open, which is the normal state while working, so verification was previously a choice between closing the editor and not checking. That makes a large refactor unsafe. The simulation touches exactly one Unity type, and only as a serialization marker, so compiling it alone costs four stub files. Faithfulness is established by the fingerprints, not assumed. Chosen on the owner's behalf. | The simulation starts needing real engine types, or Unity ships a runner that tolerates an open editor; then the stand-ins stop being honest and the harness should be retired rather than grown. |
| An index of who and what is standing where (2026-09-21) | `UniformGridIndex` buckets people and loose objects into one-metre cells, so "who is near here" reads a few cells instead of everybody. Each cell keeps its items in ascending order and a gather merges them, so every answer arrives in the same order a full scan would give -- which matters because the run's randomness is one shared stream and visiting two people in the other order would change every later draw. The index answers with candidates, never conclusions: each caller still applies its own exact test, so no rule changed. It is updated as things move rather than rebuilt per tick, because a movement resolved for one person is what the next person's move is checked against; `AgentBody.Position` and `PhysicsBody.X/Z` now have private setters so a new way of moving something cannot be written without noticing. Converted: agent and object blocking, steering and avoidance, following, noise, doorway blockages, spot-finding, and the nearest-chair and nearest-item searches. Heating now reads a live list of what is alight rather than every object and table. | All ten replay fingerprints are unchanged, so this altered nothing a player would see. Measured headless, seeds fixed, a busy office with nobody escaping, milliseconds per tick before and after: 20 people 0.073 -> 0.034; 50 people 0.217 -> 0.087; 100 people 0.277 -> 0.233; 200 people 0.732 -> 0.342. Roughly twice as fast, and the costs that grew with the square of the crowd are gone. What remains is a large cost that grows in step with the crowd rather than faster -- each person tests up to eight candidate steps, and every test walks all the rooms, doors and tables. | The next measurement shows the per-step geometry checks dominating; they are fixed by giving each grid cell the short list of rooms, doors and tables that touch it. |
| The floor as squares, and no more "room zero" (2026-09-21) | `NavigationGrid` draws the floor as 250 mm squares and records, for each one, which room it belongs to and the real distance in millimetres to the nearest wall or table edge. Walls are built with their doorways taken out of them, so a doorway is a gap rather than a thinner piece of wall. Clearance is a true distance, not a count of squares: rounding it to squares can report the middle of a 600 mm doorway as too narrow, which would seal a room and burn everyone in it in silence. A floor plan with a doorway nobody could fit through is now refused when it loads, naming the door. The three places that answered "room zero" when they could not tell which room something was in now answer with the room the point is standing in, or the room it is nearest to. | Nothing crosses a room today: people route door to door in straight lines, so anything not reachable in a straight line was cut from the behaviour rather than fixed. Squares are the smallest thing a route can bend around. Room zero is the office the fire starts in, so a box knocked into a doorway at the far end of a building could be shoved back inside the office's walls, right across the floor plan; the prototype never showed it because almost everything happens in room zero, which is why it survived. All ten replay fingerprints are unchanged, so neither change altered anything a player would see today. | The grid has to follow doors opening and tables being smashed, which is when routes start being worked out on it rather than door to door. |
| Finding the way across a room (2026-09-21) | People no longer point themselves at what they want and walk. `Navigation` answers "which way from here to there?": straight at it when they can see it, and otherwise downhill along a `FlowField` -- the cost of reaching a goal worked out for every square of floor at once, so a hundred people heading for the same door read one field and a big crowd costs no more to steer than a small one. Headings come from how cost falls away across all eight neighbours together, not from the single cheapest one, because a single neighbour puts everybody on one of eight compass headings and a crowd forms visible diagonal lanes; ties break toward the way the person already faces so nobody twitches crossing between squares. At most eight fields are worked out per tick -- a fixed count, never a time limit, which would make the same scenario play out differently on a faster machine -- and anybody asking after that heads straight at what they want for now and asks again next tick. Separately, giving up on a way out now counts whether or not the door is open. | Nothing could cross a room, so behaviours that needed to were cut rather than fixed. The give-up flaw was found by this change rather than caused by it: the timer was only read when a door was shut, so somebody wedged on the approach to an open one gave up on it, immediately picked it again, and stood there until the building burned down -- seed 3 had a person rooted to one spot for five seconds. Measured headless, busy office, ms per tick before this whole refactor began and after: 20 people 0.073 -> 0.043; 50 people 0.217 -> 0.103; 100 people 0.277 -> 0.277; 200 people 0.732 -> 0.296. Fingerprints re-recorded and the compatibility version bumped to 26. | A floor plan is big enough that working out one field is too much for a single tick; then a field is built a piece at a time across ticks, still by a fixed count rather than a clock. |
| Errands that cross the building (2026-09-21) | Fetching an extinguisher and taking it to the fire, hitting an alarm, finding a chair, hauling somebody to a way out and wandering off for a change of scene all now look for the nearest one a person could walk to rather than the nearest one in the room they are standing in. Four rules had to change together for that to work: a doorway is walkable for anybody on an errand, not only for somebody running for a way out (that single rule is why a calm person could never leave the room they started in); the wall a doorway sits in stops pushing an errand-runner away from it; a momentary stagger no longer abandons the errand, only being knocked off your feet does; and fetching is given long enough to cross a building rather than a room. | Every one of these was switched off deliberately, because setting off for something in another room walked people into the wall between. With routing there is no reason left. Measured in a two-room test: somebody brave carries a bottle through the doorway and sprays a fire in the next room, which was impossible by rule before. Fingerprints re-recorded, version 29. | The errand list on `Agent` has to be kept up when a new kind of errand is added; when that becomes a nuisance, an errand should carry its own "this may take me through doors" rather than being recognised from the activity. |
| What a thing is for, written down once (2026-09-21) | An object kind's table row now says whether people can sit on it and whether it is equipment rather than clutter, instead of nine rules each naming the kinds they cared about. The things a frightened person might do instead of running -- follow a leader, fetch an extinguisher, see to somebody, hit an alarm, wedge a door -- are an ordered list built in one place behind one small interface, rather than a chain of if-statements inside the panic rules reached through four setters wired up after construction. | Adding a new kind of interactive thing meant editing between thirteen and seventeen places, which is what makes new content expensive and is the second of the two ceilings in the way of bigger scenarios. Sitting and equipment are now a row in a table; priority order is one line naming five things in order, with the reason for the order beside it rather than buried in the fifth if-statement. All ten fingerprints unchanged, so nothing a player would see moved. | The list of errand activities on `Agent` and the kind table start disagreeing, or a kind needs a behaviour rather than a flag; then an object should carry what it offers rather than being asked what it is. |
| Proving the rules are general (2026-09-21) | `FireReactionBigBuildingEditModeTests` builds thirty rooms in a six-by-five grid joined by fifty doorways, with one way out in the far corner, and checks that it loads, that every room can be walked to from every other, that crossing it is further than the straight line through the walls, and that somebody set on fire in the first room gets at least halfway across towards the way out. `CrowdScaleMeasurements` fills a busy office with 20, 50, 100 and 200 people who cannot escape and reports milliseconds per tick. | Every other test runs against the four-room prototype, which has been tuned and re-tuned until it works -- which is exactly why it cannot show whether a rule is general. A floor plan somebody has been fixing bugs against for weeks hides any rule that only works on it. Measured after the whole refactor, ms per tick: 20 people 0.046; 50 people 0.146; 100 people 0.326; 200 people 0.356, against a budget of 20 ms. Before any of this work the same measurements were 0.073, 0.217, 0.277 and 0.732, so two hundred people finding their way round furniture now cost half what twenty people walking in straight lines used to. | A measurement passes about 5 ms a tick, a quarter of the budget, with drawing still to pay for. The next step then is Burst and Jobs on the movement and steering loops, which the simulation stays plain C# in order to keep possible. |
| Faults found by reviewing the navigation work (2026-09-21) | Eleven, fixed. The two that mattered: the queue route-finding works through held one entry per square, so a square reached a second time by a cheaper way overwrote its own link and broke the chain -- squares lost, others counted twice, and the search able to run away; it is now linked both ways so a square can be taken out and put back. And it had one bucket per possible cost, sized for a route no longer than the building is wide plus tall, which silently dropped anything past that: a floor plan that wound about reported whole wings as unreachable. Eight buckets used round and round now hold any route. Also: the squares are redone around a smashed table or a blasted hole, so both become ways to walk; "floor beside the flames" actually looks for floor rather than clamping a point into the room it was already in; alarms and chairs are walked to the way they are chosen; and a leader works out one set of distances rather than one per extinguisher. | Every one was a thing that silently did nothing or silently did the wrong thing, which is what the whole refactor was about. The queue faults in particular were invisible in a four-room building and would have appeared as "people sometimes ignore half the map" in a big one. Three regression tests were added and each was checked against the old code first. Fingerprints re-recorded, version 31; speed unchanged at 0.344 ms a tick for two hundred people. | Redoing the squares after a change is a patch around the change, which is cheap; if a floor plan ever changes shape every few ticks it should be a queued patch instead. |
| The physics engine (2026-09-21, the owner asked for full 3D physics) | Unity's built-in 3D physics (PhysX) moves everything: people, loose things, thrown things. Before, the game used its own flat, whole-number rules. PhysX runs in a private physics world for each run, and the simulation steps it once per tick (`PhysicsScene.Simulate(0.02)`). Only `Runtime/Simulation/PhysicsWorld.cs` talks to PhysX. Everything else still works in whole millimetres, read back after each step. **Considered and not chosen:** extending the in-house rules to 3D (that amounts to writing a physics engine, with worse results); Unity Physics / DOTS (deterministic and fast, but it needs Unity's data-oriented Entities framework, which the deferred-technology table rules out until profiling asks for it); the new low-level Box2D (flat only, experimental, no determinism promise). | Tumbling chairs, flying bags, crowds that really push, and crushes that emerge rather than being scripted all need real 3D contact. PhysX already ships in the project (`com.unity.modules.physics`), so no package was added. [Physics](https://docs.unity3d.com/6000.3/Documentation/Manual/PhysicsSection.html), [multi-scene physics](https://docs.unity3d.com/6000.3/Documentation/Manual/physics-multi-scene.html), [PhysicsScene.Simulate](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/PhysicsScene.Simulate.html) | Standalone profiling shows the physics step over 3 ms a tick at 500 people and 1,000 things after the cheaper fixes (fewer solver passes, sleeping, fewer colliding pairs). That is the trigger to try Unity Physics / DOTS. |
| Same seed, same run, with PhysX (2026-09-21) | The seed and fingerprint system stays, as the owner asked. It is guarded by: **Enhanced Determinism** on; bodies created in ID order; forces applied and results read in ID order; fixed solver passes (**16** position, **4** velocity); a fixed 0.02 s step; and every contact read through `Physics.ContactEvent`, merged per pair and sorted. "Two things just started touching" is worked out by the game from its own list of last tick's touching pairs, because PhysX's own flag changes between identical runs after a shape is resized. Contact modification was tried and dropped: it broke identical replays. `Editor/ProjectPhysicsSettings.cs` keeps Enhanced Determinism, Improved Patch Friction and the standard solver switched on (menu **Paniq > Project > Apply Physics Settings**). All ten fingerprints were re-recorded once for compatibility version 32. | Retrying a level must show exactly what the player's change did, and a bug report ("seed 42, 37 s in") must happen again. PhysX promises the same result on the same build and kind of machine with these settings. [Physics settings](https://docs.unity3d.com/6000.3/Documentation/Manual/class-PhysicsManager.html), [ContactEvent](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Physics.ContactEvent.html) | Replays must match across different kinds of computer (for example, shared online replays). PhysX does not promise that; Unity Physics does. |
| Standard solver, and friction that means what it says (2026-09-21) | The standard solver (PGS, "projected Gauss-Seidel"), with **Improved Patch Friction** on. The alternative solver (TGS) was tried twice. Both times people sank further into each other and a laptop stuck in a table. Without improved friction, things slid about half as far as the settings said. | The standard solver gave the fewest overlaps in the tests, and friction now matches the per-kind numbers. | A future Unity version changes solver behaviour, or stacking tall piles needs TGS's stiffness. |
| People as bodies (2026-09-21) | Each person is a solid, 12-sided column, **250 mm** in radius and **1.7 m** tall, weighing their trait mass. Standing, they cannot tip or rise (rotation and height locked) and their feet slide freely. A motor gives them at most **3 mm/tick²** of push toward where they want to go, so a strong crowd carries a person along, and one person cannot barge through a wall of people. A capsule was tried first: its round bottom wedged things underneath it, and it climbed onto boxes. Seated people are fixed in place and ignore their own chair. After getting up with nowhere to step, they keep ignoring it until they are **600 mm** clear. | Pushing, being carried by a crowd and blocking a doorway all come from contact now, not from rules. | People need arms and legs that react (ragdolls), or 500+ people cost too much. Then simpler bodies for people far from the camera come first. |
| Falling down and getting up (2026-09-21) | Going down lays the body flat at once, along the first direction with room to lie. The display draws the fall over **0.22 s**. With no room anywhere, the person stays upright in the physics ("crumpled") and is drawn slumped on their knees. Getting up looks for a clear spot nearby and never picks one through a wall. A blast tips the person over before pushing, so the lift is not lost to the standing lock. A body lying down is slow to roll (angular damping **4**). | Letting PhysX tip a tall column over took it through the floor and into neighbours. Laying it down in a checked spot is safe and still looks like a fall. | The display gets animated characters that can play a real fall. |
| Crushes (2026-09-21) | Each person adds up how hard everything presses on them each tick. Pressure over **700** for **25** ticks in a row (half a second) knocks them down with a new `AgentCrushed` event (appended to the event list, so older event numbers are unchanged). | Doorway crushes now come out of the crowd itself; no crush rule knows about doors. | Playtesters find crushes too common or too rare. `CrushPressure` and `CrushTicks` are scenario dials. |
| Nothing lighter than 10 kg to the engine (2026-09-21) | Loose things are given to PhysX at no less than **10 kg**. The game's own rules (breaking, knock-downs, throws) still use the real mass. | A 1 kg laptop squeezed between two 80 kg people was pushed straight through them. PhysX copes badly with very different masses pressing together. | A thing needs to feel feather-light in the physics itself, for example paper blowing in a blast. |
| Keeping bodies apart (2026-09-21) | Overlapping bodies are eased apart at no more than **2 m/s**. Walls are **40 mm** thick. Movement uses speculative collision checks (ContinuousSpeculative); a stricter mode made people pass through walls. A thrown or dropped thing ignores the person who let go until it is clear of them. Things being held, and spares not yet placed, are not solid. The `PressWatch` test helper fails any test in which two bodies stay pressed more than **75 mm** into each other for more than **2** ticks. | Separation that is too fast fires people across the room; too slow and they stay inside each other. The tests measure what the engine measures instead of the old circles. | Overlaps show up in playtests. |
| Physics feel dials (2026-09-21) | A new `Physics` group in the scenario, with whole-number dials: gravity **150%**, blast strength **100%**, blast lift **60%**, throw strength **100%**, throw arc **20°**, tumble **100%**, floor grip **100%**, top speed **400 mm/tick**, wall height **3 m**, table height **740 mm**, person push **3 mm/tick²**, person floor grip **60%**, crush pressure **700** for **25** ticks, drag slack **600 mm**. Gravity is 150% because real gravity made thrown things hang in the air long enough to look floaty from the camera's height. | The owner wants fun rather than realism. These are the numbers to turn. | Presets ("Cartoon", "Heavy") and live tuning while playing are the next step (Phase 4 of the physics plan). |
| No cause, no harm (2026-09-21) | A thing nobody has pushed, thrown or blasted (no causing event) cannot hurt a person, break another thing or smash a table, however it is moving. Every throw and launch logs its own event. | PhysX nudges resting things by fractions of a millimetre. Without this rule, a chair settling against a wall could "hit" someone, and the story of the run would have an event with no cause. | Never, while the causal event log is the game's explanation of what happened. |
| Tests that need Unity's physics (2026-09-21) | `Editor/TestBridge/TestBridge.cs` watches `Temp/PaniqTestBridge/request.txt` and runs the tests inside the editor that is already open. `tools/RunUnityTests.ps1` writes the request and prints the results (`-PlayMode`, `-Filter`, `-Category`, and `-Reset` for a run Unity dropped). `tools/CompileAgainstUnity.ps1` checks that everything compiles without Unity. The plain-.NET runner (`tools/RunEditModeTests.ps1`) now reports tests that need PhysX as **skipped**, never as passed. At most **8** physics worlds may be open at once in the editor; each test's worlds are closed after it. | PhysX exists only inside Unity, and the editor locks the project against a second copy of Unity running tests from the command line. [Test Runner API](https://docs.unity3d.com/Packages/com.unity.test-framework@1.4/manual/reference-test-runner-api.html) | Tests move to a build machine. Then the same tests run with Unity's batch mode. |
| Particle effects (2026-09-21, the owner asked for effects held to the physics bar) | Unity's built-in Particle System (Shuriken), not VFX Graph. Eleven particle systems are built once when the game starts, one per kind: sparks, bang smoke, debris, dust, splinters, shards, flames, fire smoke, embers, foam and mist. Nothing is created mid-game. The code places each particle itself. For a one-off effect (a bang, a breakage, a knock), where each particle goes comes from the event's ID, so a replay looks the same. Each particle is a small cube in the prototype's chunky style, drawn with `Sprites/Default` (already on the always-included shader list, so it works in a built game) and lit by nothing. Particles bounce off the floor only. The display's walls are not solid, so a spark can fly through one. Burning things keep an orange glow behind walls, from a box drawn only with the see-through fire material. Particles never touch the simulation: they live in the display's scene, not the run's physics world. | Bangs, fire, foam, dust and splinters make the physics readable. The Particle System is already in the project (`com.unity.modules.particlesystem`) and handles thousands of particles cheaply. VFX Graph would need a new package and pays off only at tens of thousands of particles. [Particle System](https://docs.unity3d.com/6000.3/Documentation/Manual/Built-inParticleSystem.html), [choosing a particle system](https://docs.unity3d.com/6000.3/Documentation/Manual/ChoosingYourParticleSystem.html), [ParticleSystem.Emit](https://docs.unity3d.com/6000.3/Documentation/ScriptReference/ParticleSystem.Emit.html) | Particles cost more than **2 ms** a frame in the worst blast, or an effect needs building-filling smoke. Then VFX Graph. A softer, non-cube look is the owner's call. |
| Particle budget and dials (2026-09-21) | `Content/ParticleEffects.asset`, linked from the prototype scene, holds each effect's amount, size, lifetime, speed and start and end colours, all as percentages of the built-in look. Changes show on the next effect, even while playing. There is one shared budget of **8,000** live particles. Past half of it, new effects make proportionally fewer; at the budget they make none. Each kind also has its own cap (sparks 1,500, flames 2,500, fire smoke 2,000, foam 1,500, the rest 500–800). | The worst moment (TNT beside a burning, sprayed room) must not slow the game. The owner can tune the look without code. | Profiling shows headroom to spare, or the budget visibly starves effects in play. |
| Physics feel presets and live tuning (2026-09-21) | Two presets in `Content`: **Cartoon** (the scenario's own values, and what the prototype level starts with) and **Heavy**: gravity 100%, blasts 65% with 30% lift, throws 75% at 12°, tumble 40%, floor grip 140%, top speed 300 mm/tick, person push 2 mm/tick², person grip 90%. Swap one in by dragging it into the **Physics Feel** slot on the Fire Reaction object in the scene. A preset's values become the run's scenario data when the run starts, so a run played with one replays like any other. **Live Physics Tuning** (a checkbox beside the slot, editor only) copies the preset's dials into the running game every tick, so you can change them while playing. Floor grip, top speed and wall and table height are built into the world at the start and only change on the next Play. The stats panel (Tab) names the feel in use and says when a run was tuned live and cannot be replayed. The blast-strength dial now scales how far people are thrown as well as things. | The owner asked for tuning toward fun. Comparing two feels side by side, and turning dials while watching, is how "fun" gets found. | Playtesting settles on a feel. Then fold it into the scenario and drop the presets that nobody uses. |
| Physics profiling (2026-09-21, standalone) | `Scenes/StressProfile.unity` and menu **Paniq > Profiling > Build Stress Profile Player** build a player that measures and quits. It uses a 30-room building of 6 × 6 m rooms, full of busy calm people and twice as many boxes, and a storm of particle effects worse than any real moment. **Results** on an Intel Core Ultra 7 265K (20 threads) with an RTX 5070 Ti, release build: 100 people and 200 boxes, **1.12 ms** a tick (physics 0.92); 200 and 400, **1.93 ms** (physics 1.60; budget 5 ms for the whole tick, **met**); 500 and 1,000, **4.31 ms** (physics **3.44**; budget 3 ms, **missed by 0.44 ms**; slowest tick 5.66 ms). Particles cost **0.66 ms** a frame in the storm (budget 2 ms, **met**), with at most 4,125 alive. Halving the solver passes (16/4 to 8/2) saved almost nothing in the editor (4.36 to 4.28 ms), so the passes stay; the cost lies elsewhere. | `AGENTS.md` requires standalone numbers before adopting scale tooling, and whenever a stone raises what is on screen. The prototype level has 20 people; 200 fit easily. | The level grows toward 500 people. Then profile inside the step first (the engine's own work, against the contact report and reading back 1,500 bodies each tick), then try sleep thresholds and fewer colliding pairs. Unity Physics / DOTS comes only after those. |
| A way out that has just opened (2026-09-21) | A door that becomes a way through — opened by anybody, battered down, or blown open with TNT — is recorded, and at the end of that tick `DoorBehaviour.AnnounceWaysOut` tells the crowd. Two tiers: everybody who can still walk there stops believing that door would not open, and the people in the door's own room or the room straight through it also think again on the very next tick. A door somebody shut themselves is exempt from the first until somebody else opens it. End of tick, ascending door then ascending person, and no random draws. | The player's one real move is opening a door, and nobody noticed until their own next thought came round, up to 1.2 s later. The two tiers keep it believable: a false memory is a bug, but a whole building turning on its heel the instant a latch clicks two rooms away reads worse than the problem it fixes. Placed at the end of the tick for the same reason doorway blockages are, so the next tick reads one settled answer. **Chosen on the owner's behalf:** the two-tier split. | People bolt so hard that the queue at the one doorway stops being interesting. |
| An open way out is never crossed off (2026-09-21) | In `ChooseExitDoor`, a door leading outside that is standing open is no longer excluded by this person's memory of having found it shut. A door partway along the route, or one they shut themselves, still is. The open-door bonus rose from 4 m to 25 m and is now withheld when the room that door is in is alight; the current-choice bonus rose from 1.5 m to 4 m and the noise in the choice fell from 1.5 m to 0.5 m. | Somebody who had walked to the locked exit and given up went on ignoring it for the rest of the run, even once it stood wide open. 25 m beats the longest walk in the building (about 23 m), so a way out you can see open always wins on distance; the guard stops that marching people into a burning room. Less noise because dithering between two doors is only human when the choice is close. **Chosen on the owner's behalf:** all four numbers. | A floor plan with several ways out where the choice should look less decided. |
| Eagerness (2026-09-21) | Somebody with a route to a way out they can see standing open does not roll for hesitation or a swerve, drops a swerve already running, does not drift with the crowd, and runs rather than walking even if a bell had left them composed. Routed through the existing `TraitEffects` chance functions, which already return 0 for the composed — and because `Pcg32.NextPercent` draws whether or not the answer is zero, this costs no change in the number of random draws. Overridden by being frozen, alight, inside their own danger distance of the flames, or in the middle of a rescue. | The owner: "when a clear escape route presents itself, the agents should be very eager to use it… most agents have high self preservation", and chose "almost nobody dithers". Near-universal rather than trait-gated for that reason; what is left of personality is how fast they run, how soon they notice, and who stops for somebody else. **How this is measured, and a correction.** I first reported this as "people out within 15 s went from 37% to 42%", and the owner rightly said that is no difference. It was also the wrong measure: the denominator is everybody in the building, including ten people with a 25 m walk and three single-file doorways in the way, so it is dominated by queue length rather than willingness — twenty people through one 1 m gap is around ten seconds of queue before anybody's intent matters at all. The owner's rule is about *trying*: "if there is an unobstructed exit… most of them should try to bolt to the exit. Then if something else obstructs, like chairs, boxes, other people — that is other emergent behavior." So the measure is intent, counted every tick for every scared person on their feet while the way out stands open and clear, with somebody stuck in a queue counted as trying. Over 7 seeds × 5 opening moments: **58% heading for the way out before this round, 66% after**. Of the 34% who are not, 25 points are people deliberately doing something else — frozen with fear 12, helping somebody 6, alight 5, fighting the fire 2 — so **roughly nine in ten of the people not otherwise occupied are heading for it**. | The crowd reads as one organism rather than twenty people. |
| A chair in a doorway still jams the door (2026-09-21) | Anything resting in a doorway stops that door dead, from either side and both ways, exactly as before. A door leaf now swings away from whoever pushes it and each door carries a `DoorSwingRule` (both ways by default), but that decides which way the leaf goes, never whether a wedged door opens. What changed instead: the HUD says a door is wedged when you point at it, and a leader now sends somebody strong enough at a wedged door as well as at a locked one, without having had to try it themselves. | I first made a one-sided obstruction leave the other swing free. Measured, that nearly tripled how often the player's clicks worked (the two clicks failed to open the only way out in 15 runs of 35, down to 3) — and the owner rejected it: "a chair blocking a door is just good emergent simulation." They are right, and the real fault was never the jam. It was that the player clicked, saw the door turn green, and got silence. With the jam kept and the message and the leader rule added, failures fall from 15 of 35 to 9 of 35 because the crowd now clears some jams itself. **Chosen on the owner's behalf:** using the obstruction shove strength (7) rather than the door-breaking strength for who gets sent. | A jam early in a run with no influence left becomes an unrecoverable loss often enough to be unfair. |
| A table that tips over (2026-09-21) | A collapsed table becomes a real loose object rather than vanishing from collision: one dormant `TableWreck` slot is reserved per authored table (the same spare-slot pattern as a spare extinguisher), activated at the table's place with half its short side for width (clamped to 350–500 mm), 40 kg, and a share of whatever knocked it over, by mass. It never catches fire of its own accord, because the table it came from keeps its own burning. Presentation tips it up on one edge, with the direction taken from the table's own ID. | The owner: "the table breaking looks and acts a bit strange. Better have it just act like a physics object and tip over." It cannot land inside anybody, because until the moment it broke the walking rules kept everything out of the table's rectangle and the heap is at most half the short side centred on it. Left at the table's full width, the two halves of the meeting table wall the room in two and cost 11 points of escape rate; halved, they cost 4, which is the price of the room genuinely changing shape. **Chosen on the owner's behalf:** the mass and the size clamp. | A heap should be several pieces rather than one. |
| Clipping through boxes was a drawing fault (2026-09-21) | Anything drawn square is now drawn at 1/√2 of its footprint's width, so its corners land on the round footprint the simulation keeps clear instead of two fifths of a radius outside it. Display only. Separately, three real ways a thing could become solid inside somebody are closed: a thing falling off a table with nowhere clear to land now stays up there, everything a collapsing table held now looks for clear floor instead of dropping where it stood, and settling into a chair is refused if something is in the seat. Superseded on the physics version, where things are drawn at their real physical size: see "Eager escape on top of physics". | The owner reported clipping through boxes. Measured on the unchanged code, a person is **never** inside a thing: 0 readings over 17,500 ticks across seven seeds. So the physics was already hard and the mismatch was that a box is drawn as a cube as wide as its footprint is round. I did try an end-of-tick sweep that pushed things out of people; it measured worse (0 readings became 161) and was removed. The lesson is recorded because the instinct to "harden" was wrong and the measurement is what caught it. | Footprints stop being circles. |
| Stop, drop and roll (2026-09-21) | Each time somebody alight would lurch off in a new direction, a 4% chance that they throw themselves down and roll instead — about two people in five over the ten or so lurches they get. The roll lasts 1.2–2.4 s and, when it runs its course, one draw at 35% puts the flames out. Only somebody on their feet can drop, so being knocked over while alight never becomes a roll. Putting a fire out now also returns the person to fleeing (or to frozen, if their freeze had not run out), which it failed to do before — a doused person was left in the burning activity for good. | The owner asked for it. One draw at the end rather than one a tick so the odds are the number as written and can be read off; measured over seven 60 s runs, 132 people caught fire and 11 went down to roll. **Chosen on the owner's behalf:** both percentages and the duration. | Rolling saves so many that catching fire stops mattering. |
| A jet that sweeps (2026-09-21) | While the trigger is held, the aim swings either side of the target as a triangle wave over 2.5 s, with a phase drawn once as the trigger goes down. The swing is 24° less 3° per point of strength, so an ordinary person waves 9° either side and anybody 8 or stronger holds it dead straight — the same shape as the recoil rule. | The owner: "using fire extinguishers is very static in one direction." Applied to the body's aim rather than only the cone, so the person turns and the drawn jet follows for free. One draw per trigger-pull, then arithmetic. **Chosen on the owner's behalf:** the width, the falloff and the period. | The jet reads as wobbly rather than worked. |
| Sitting on the seat (2026-09-21) | A seated person is lifted to the 0.45 m seat and squashed to 62% of their height, so their head clears the 0.74 m table instead of floating over it; they were previously sunk 0.16 m into the floor. Settling into a chair also scoots it 120 mm the way it faces, stopping against whatever is in front of it, which mirrors the shove back that getting up already gave. | The owner: "agents now sit on chairs at ground level. Should be on the seat itself", and "getting out of a chair should scootch it back, and same in reverse." The pose is display only; the scoot is simulation, because the chair really moves. **Chosen on the owner's behalf:** the squash and the scoot distance. | Chairs get real seats people can be placed on. |
| Backing out of a queue at the only way out (2026-09-22) | `AvoidCrowdedExit` marks the door somebody is queueing at as one to avoid for 2–4 s, which also hides it from the route search. It now does that only when another way out of the building is actually reachable without it. When it is the only way out, the door stays on their route and instead the give-way rule applies — stepping aside for half a second to a second and coming again — which it now does at a shut door too, and from anywhere inside the commit distance rather than only within a metre. | Measured every tick over 7 seeds × 5 moments: 5% of scared people on their feet were fleeing with *no route at all* while an unobstructed way out stood open, because the rule meant to send them to a different door had hidden the only one there is. That is 3% now, and the share heading for the way out went 65% → 66%. Simply switching the rule off did better on paper (69%, no route-starvation at all) and was rejected: it let somebody stand perfectly still in a crush for **22.6 s**, against 2.5 s before and 2.3 s after. Stepping aside is what keeps a crush moving; being sent away is what makes people give up. **Chosen on the owner's behalf:** using the commit distance for the wider give-way reach. | A floor plan with more than one way out, where being sent to the other door is a real answer again. |
| Eager escape on top of physics (2026-09-22) | The eager-escape work (people run for a way out they can see standing open, drop and roll, tipping tables, a door that says it is wedged) was built without physics and has been carried over onto the physics version by hand, not merged. Kept from the physics version where the two differ: things are drawn at their real physical size, not shrunk to fit the old round footprint; a door somebody shut themselves is remembered the physics version's way (`ShutByThem`), which it had already invented. Put back after review: the drop-and-roll chance is drawn when a burning person changes direction, as designed, not every tick (drawn every tick, nearly everybody alight dropped within a second); and a person set on a way out does not zig-zag, dither, or walk calmly. Not carried over: sparks and smoke from a bang showing through walls, because the new particles use one material that walls hide. The box-overlap test now accepts a thing kicked out of the building through an open way out, which physics makes possible and is correct. Fingerprints re-recorded; compatibility version **33**, content revision **41**. Standalone stress profile afterwards (2026-09-22, same machine as "Physics profiling"): 200 people **1.90 ms** a tick (was 1.93), 500 people **4.18 ms** with physics **3.34** (was 4.31 and 3.44), particles **1.14 ms** a frame in the storm (was 0.66, still within the 2 ms budget; the particle code did not change, and the whole storm frame was faster, 1.39 ms against 1.76, so this is run-to-run variation). | The owner asked for both pieces of work in one game, with nothing that worked before broken. Checked against both starting points: every test that passed on the physics version still passes, all of the eager-escape version's tests are present and pass, and two runs in a row give the same ten fingerprints. | Playtesters miss seeing a bang in the next room. Then give the particles a second, see-through material, the way flames already have one. |
| Through a door means through, and nobody waits for nobody (2026-09-22, the owner reported seed 42) | Two faults, both at doors. **Going back through a door:** somebody who had stepped through the door they were running for, and was then knocked off their line, still aimed for the near side of it, walked back through, and was too close to it to think again, so they went back and forth. Now, once somebody stands in the room on the far side of the door they were heading for, they choose their next move from that room at once (`DoorBehaviour.HasPassedThrough`). **Waiting for nobody:** somebody stuck near a door stepped aside "for whoever is lined up with the gap" without checking anybody was. When everybody round an open way out was stuck, everybody stepped aside, the gap stood empty, and they waited there for good. Now somebody steps aside, and keeps standing aside, only while somebody else on their feet is lined up with the gap within about a metre of it (`SomebodyElseLinedUpAt`). Compatibility version **34**, content revision **42**, fingerprints re-recorded. | In seed 42 the owner let two people out of a corridor a cruel person had locked, with the way out standing open, and they ran about instead of leaving; somebody else circled beside the open way out. Both now get out. Two tests (`SomebodyKnockedAsideJustThroughADoor_CarriesOnRatherThanGoingBack`, `AKnotBesideAnOpenWayOut_DoesNotWaitForNobody`) were checked to fail on the old code first. | A crowd at a door gets so dense that nobody ever lines up with the gap. Then the queue needs an order (who goes next) rather than politeness. |
| Clearing the way is self-preservation (2026-09-22, the owner reported seed 42) | Anybody frightened who is stuck with a loose thing in their way grabs it and throws it clear, if they can lift it (5 kg plus 2.5 kg per point of strength, so most people can lift an office chair). Two places: at a door with something wedged in it, and stuck in a crush with something right in front of them (`DoorBehaviour.TryClearTheWay`). It goes back past them and off to one side by **30-70 degrees**, within **400 mm** reach, and may land on the people behind. Only something too heavy to lift still needs somebody strong (strength 7 or more) to heave it along the wall, and a leader still sends somebody for that. Before, only the strong could shift a wedged thing, and only by heaving, which a pressing crowd stops dead. The test watcher that fails two bodies pressed deeper than 75 mm now allows that for **3** ticks rather than 2: somebody knocked flat while running skidded a light chair along the floor at 76-81 mm for three ticks (seed 47) and came apart cleanly. Compatibility version **35**, content revision **43**. | In seed 42 an office chair jammed the way out and the meeting room waited behind it for 64 seconds while four people died; the owner asked that somebody throw it out of the way without needing a leader. Now the first person out goes 2 seconds after the door is unlocked. `AChairWedgedInTheWayOut_IsThrownClearByAnOrdinaryPerson` fails on the old code. | Throwing things back into the crowd hurts too many people in playtests. Then throw to the side along the wall instead. |
| Tables are bodies like everything else (2026-09-22, the owner asked) | A table is no longer part of the building. Each one is a solid block from floor to top with a weight from the floor it covers (**25 kg per square metre**: a 1.2 x 0.7 m desk is 21 kg, the 5.4 x 1 m meeting table 135 kg), and the engine shoves, turns and tips it like any loose thing. A blast shoves it too, shared out by weight against a 20 kg thing, so a bin flies where the meeting table barely rocks. Everything else still sees a table: a thing that meets one meets `StaticKind.Table`, so what smashes a table and what bounces off it are unchanged. The game follows each table after every step: its footprint (the box round it, whichever way it is turned) and its pose, and the walkable squares are worked out again once it has shifted **150 mm**. The display draws it where the physics has it and eases towards that, instead of the hand-animated tip-over that came across from the eager-escape branch, which turned oddly and sank through the floor. A smashed table still leaves the heap of boards, and the table itself shrinks away. **Measured** (2026-09-22, standalone, same machine as "Physics profiling"): the measuring building now has a desk in each of its thirty rooms, which it never had before. With them, 200 people cost **2.32 ms** a tick (physics 1.57) and 500 cost **8.97 ms** (physics 3.56); without any desks the same build measures 1.90 and 4.15. Almost all of that difference is the game working out routes round furniture, not the engine. Working the walkable floor out again only when a table comes to rest, rather than every tick it is being shoved, took 200 people from 3.35 ms back to 2.32. The prototype level has four tables, not thirty. | The owner asked for tables with no special physics -- "just move and react like everything else" -- after seeing the old tipping animation. Now a crowd pressed against a desk shoves it, a blast turns one over, and what used to be a drawn effect is the real thing. Two test tolerances moved with it: a person may lean **40 mm** into a table (they push tables about now), and a kicked chair is expected to shove a table rather than bounce off it. | Tables need to be walked under or climbed on, or a level has so many that following them costs real time. Then give them proper legs and a top rather than one block, and only follow the ones that moved. |
| Sitting down happens in parts (2026-09-22, the owner asked) | Nobody snaps onto a seat any more. They walk to a spot beside the chair, **75 degrees** round from the way it faces and clear of it; pull it out **300 mm** over **12 ticks**, the engine carrying it so it stops against whatever is behind; then lower themselves onto the seat over **10 ticks** while the chair slides back in under the table, stopping a person's width short of it. Getting up on purpose runs the same way round: they let go of the chair, back out on it, and rise onto a clear spot. Somebody who leaps out -- frightened, knocked out of it, or alight -- skips all of that: the chair is shoved backwards at **30 mm a tick**, lifted a little and set turning **12 degrees a tick**, so it goes over on its back in about half a second. Both numbers came down after the owner saw tables breaking when people stood up: at twice the speed the chair clattered into the meeting table hard enough to smash it, and at twice the spin it whirled through whoever stood behind it before the engine could push them apart. What tips it over is the lift and the spin, not the speed. Both kinds of chair follow the same rules; the castor ones simply roll further. A body knocked flat now keeps **40%** of its speed rather than all of it, so a falling runner skids instead of ploughing along the floor into whoever is standing there. | The owner: "agents sort of just snap in and out of them, not really scootching them back to get on or off, and when surprised and jump out of the chairs, chairs should fall back". Two tests cover it (`SittingDown_PullsTheChairOutAndRidesItBackIn`, `LeapingOutOfAChair_SendsItOverBackwards`). A chair being pulled cannot be "free" for somebody else to take, and the person riding one does not block it. | Sitting needs to look right at a packed meeting table, where there is no room to pull a chair out behind you. Then people should shuffle in from the side instead. |
| Prototype presentation camera | Orthographic camera at a 45-degree horizontal angle and 35.264-degree elevation | This is the familiar isometric game view and keeps the whole 12 m room readable. | Playtesting shows that depth or agent readability requires a different camera. |

Run **Paniq > Project > Configure URP** after Unity resolves packages. It
creates and assigns Unity-version-matched URP assets; commit its generated
assets and project-settings changes. On a project that already has URP
assigned the command does nothing, so nothing the game needs in order to draw
itself may depend on it having been run.

## Prototype decision: rounds, scoring and the design spine

Settled in a design review with the owner. The creative decisions themselves
live in [game vision](game-vision.md); recorded here are the ones with
technical consequences, plus the defaults chosen on the owner's behalf so they
can be found and overturned.

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| End of a round | A round ends when every person has reached a final outcome: out, dead, or alive and settled somewhere they will not leave | "Nobody is moving" alone never ends, because a person safe in a room the hazard cannot reach stays unresolved forever | Hazards exist that make "settled" ambiguous, such as one that can reach anywhere given time |
| Surviving inside | Counts as saved | Barricading is a legitimate way to live through a disaster, and levels are large enough that reaching an exit is not always the best plan | Playtests show hiding is dominant and exits stop mattering |
| Score | Percentage of the crowd saved, shown during the round and on the end screen | It is the game's stated result, and it is one number a new player understands without explanation | A scenario makes "saved" mean something other than "alive at the end" |
| End screen | The scene freezes and stays clickable; clicking a person gives plain facts about them | The causal event log already holds every fact needed, and a frozen scene is the cheapest way to make a missed event recoverable | The plain-language retelling replaces or absorbs it |
| Damage model | A ladder of visible states (upright, staggered, knocked down, out cold, dead, on fire), not hit points | It stays readable with hundreds of people on screen, and the existing collision and knock-out code already implements most of the ladder | A scenario needs gradual injury that a state ladder cannot express |
| Director layer | The Director is simulation, not presentation: seeded randomness, stable IDs, and it may read only simulation state | It decides outcomes, so the [simulation contract](simulation-contract.md) applies to it in full. Retrofitting this later would be painful | Never, while the simulation contract stands |
| Director reactivity | The Director reacts to how the run is going, and runs are therefore not comparable between attempts | The owner accepts randomness as a core ingredient, as in a sport. The cost is that a score is a match result rather than a personal best | Playtests show players cannot tell whether they improved, and it matters to them |
| Replayability under a reactive Director | Still satisfied: given the same seed **and** the same player inputs, a run reproduces exactly | This looks like a conflict with the simulation contract and is not. The Director consumes the seeded random source and simulation state only, so replay and debugging are unaffected | The Director is ever given access to input timing, frame rate, or anything the presentation layer knows |
| Influence source | Unchanged: the player starts with a pot and earns back only by saving people | A well-tuned Director is expected to absorb the risk that a bad opening starves the player | Playtests show a bad opening reliably ends the run as a spectator. The alternative on the shelf is for panic itself to pay |
| Modes | One mode. No separate sandbox | A large level supplies the calm, watchable half for free, so a second mode would add maintenance without adding play | Levels stay small enough that the disaster is always in view |
| Attention aids | No helper markers, minimap, or alerts. Sound is the intended replacement | The owner wants following events to be the player's job. Sound keeps that intact because it belongs to the world rather than to an overlay | Playtests show players lose the thread even with sound in place |
| Time control | The player can pause to look but not to act: time stops and the camera moves, but no Influence is spent and no card is played while paused | With no helper markers and a large level, a player needs some way to catch up when two things go wrong at once. Freezing the reading but not the acting keeps the round a real-time panic instead of turn-based tactics | Playtests show players still lose the thread even with sound and pause, or that pausing is used so constantly the round is effectively turn-based anyway |
| Hazard modelling | Every disaster is built as a mix of four families: spreading, rising, contagious, hunting | It keeps scenarios as content rather than as code, and gives a clear signal when the threat system needs extending instead of special-casing | A wanted scenario cannot be expressed as a mix of the four |
| Crowd between levels | Each level has its own cast; survivors do not carry forward | Levels are self-contained dioramas that tell a story together, so continuity comes from the story rather than from the roster | The owner wants losses to carry an ongoing personal cost |
| Camera projection | Orthographic for now, keeping the true isometric view the prototype already renders. A slight perspective is a recorded future goal | The look works today and costs nothing. The reason to move is depth of field: the miniature blur that sells a doll's house is a perspective effect, and URP's depth of field is [widely reported](https://discussions.unity.com/t/urp-orthographic-depth-of-field-bugged-and-possible-fix/1608436) to behave unpredictably with orthographic cameras. **Not confirmed against Unity's own documentation** — `docs.unity3d.com` was unreachable from the environment this was written in, so test it in the editor before relying on it | The art pass wants the miniature blur. It is one camera setting plus reframing, so an afternoon rather than a rework |
| Camera framing | The view always looks at the building corner-on, and rotation snaps a quarter turn at a time, so there are four views | It is what "45 degrees" means for Paniq, and snapping keeps every screenshot of the game consistent | Levels stop being rectangular enough for four corners to cover them |
| Camera movement | W moves the camera up the screen whichever corner the view is from, never relative to the world | Otherwise W changes direction every time the player rotates, which is disorienting | Never, unless playtests contradict it |
| Zoom | Zooming couples to tilt: closer looks more along the floor, further out looks more down on it | It is the owner's intended feel, and it makes a close view readable without a separate control | The tilt makes close-up play hard to read |
| Hidden objects | Pale blue silhouettes through walls, as built. Dropping the near walls entirely — keeping what is mounted on them — is a recorded future goal | The silhouettes work and are built. The cutaway is a better fit for the doll's-house look but needs data that does not exist yet: doors record which wall they sit in, alarms and other wall-mounted objects record only a position | The cutaway stone is planned; it starts by giving wall-mounted objects a wall |
| Character animation | Procedural only: transforms moved and bounced in code, a bob rather than a stride. No character rigs, no Animator controllers, no animation package | Crude models and minimal animation are the intended style, and the existing fire cubes already animate this way. Recording it stops an animation system being adopted by habit | A behaviour needs a motion that cannot be expressed by moving a transform |


## Prototype 2 decision: building the round

Settled or chosen while building prototype 2's first five stones on
2026-09-22. The three marked **owner** were the owner's own choice; the rest
were chosen on their behalf and are recorded here so they can be overturned.

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| How a round begins (**owner**) | The level opens calm and nothing happens until the player presses "Trigger event". No timer | The owner wanted a deliberate beginning and a chance to look around the office before setting it off. It also makes the button a part of play rather than a debug toy | A level wants a disaster already under way when the player arrives. It is one tick box on the level asset |
| What clears a level (**owner**) | 75% saved: fifteen of the twenty people. The owner asked that it stay easy to tweak | Just above what a do-nothing run manages, so intervening is worth it, while leaving room for "60% saved was a triumph" | Playtests say it is reliably out of reach or reliably trivial. It is one number on the level asset |
| What the high score remembers (**owner**) | One number per level: the best share of the crowd ever saved, in `PlayerPrefs` | The simplest thing to understand with one level, and the docs already accept that two runs are not strictly comparable | Runs become comparable, or the owner wants a per-seed record |
| Where the round lives | `RoundSystem`, in the simulation | Ending a round decides an outcome -- it is what turns the last survivors into people who were saved -- so the [simulation contract](simulation-contract.md) applies to it in full | Never, while the contract stands |
| "Settled somewhere safe" | Alive, not on fire, in a room with no fire in it, and no path from any burning room to theirs through a door that is open, broken or blasted -- held for five seconds | It is the case the design names and the only one that can end a round that would otherwise hang for ever. The five seconds stop a round ending in the lull before somebody shoulders a door open | A hazard exists that can reach anywhere given time, or that does not travel through doors |
| A new outcome value | `Survived`, appended to `AgentTerminalOutcome` | The [agent state model](agent-state-model.md) anticipated this. Appended, never inserted: a value's number is part of the replay fingerprint | Never; the rule is append-only |
| Trigger as a player command | `TriggerEvent`, appended to `PlayerCommandType`, costing nothing | Everything the player does has to reach the run as a recorded command or a replay cannot reproduce it. Free because it is not a card | Never, while replay matters |
| What a level is | A small `LevelDefinition` asset: an id, a name, which scenario, which physics feel, whether the hazard waits, and the clear target | The beginnings of a level system without building a level system. A second level is a duplicate of this asset, not new code | A level needs anything a scenario cannot express, such as its own cast or its own cards |
| Playing again | The scene is reloaded and the run rebuilt from scratch, with the chosen seed left in a static `LevelSession` that is not in the scene | The run owns a private physics world and a sceneful of built geometry. Throwing it all away is the only way to be certain nothing carries over, and it takes a fraction of a second | The reload becomes slow enough to notice, which would mean resetting in place instead |
| Pause | `Time.timeScale = 0`, with the camera reading the clock that ignores it | Everything freezes -- people, fire, smoke, sparks -- which is what "the scene freezes" should look like. The run steps its own physics world by hand, so this changes nothing about the size of a tick; it only stops ticks happening | A system needs to keep running while paused |
| Screens and buttons | Unity's immediate-mode GUI, the same drawing the existing counter and cards already use | No new package, and this prototype is explicitly "not art, menus, saving, or platform work". A real menu system is a vertical-slice job | The screens need layout, fonts or animation that immediate mode makes painful |
| Compatibility version 38 | Bumped from 37, content revision 45 to 46, and all ten replay fingerprints re-recorded | Marking survivors at the end of a round genuinely changes what a run produces, so the recorded numbers had to change. Done in the same commit, as [the workflow](development-workflow.md) requires | Every behaviour change; the procedure is the point, not this instance |
| The building's outside | Drawn as a slab, a window band and the top of the storey below, then dark | The doll's-house look wants a model of a place, and a floor plan on a black background reads as a diagram. Presentation only: no simulation change, so no fingerprint moved | The cutaway-walls stone, which will want the outside walls to behave differently as the view turns |
| Camera numbers | Pan 14 m/s (slower when zoomed in), zoom in twelve notches from the full-building framing down to a sixth of it, tilt from 35.264 degrees out to 18 degrees in, and panning allowed 8 m past the building's edge | [Look and controls](look-and-controls.md) says these are presentation values to be chosen when the camera is built. Chosen to feel unhurried at full zoom-out and still readable up close | Playtests say the view is sluggish, or that the close view is hard to read |

## Prototype 2 decision: the first playtest's changes

Settled or chosen on 2026-09-23, after the owner played the round. The rows
marked **owner** are the owner's own choice; the rest were chosen on their
behalf and are recorded here so they can be overturned.

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| Doors cost influence (**owner**) | Turning the key **50**, walking a door open **30**, pulling one shut **10**. Each click pays for what that click does, so unlocking a far-off door and leaving the people inside to open it themselves is cheaper than opening it yourself. A click nobody can pay for, or one the door refuses because something is wedged in it or somebody is standing in it, does nothing and costs nothing | Working a door was the player's commonest move and it was free, so there was never a reason not to fling every door in the building open. The owner set all three prices | The first playtest with them. The building's one way out starts locked, so opening it is **80 of the 100** the player starts with, before anybody has been saved: `InfluenceSettings.Starting` is the one number to raise if that reads as unfair rather than tense |
| Trigger stays free | Pressing "Trigger event" still costs nothing | It is the button that starts the round, not something the player spends influence on | The owner wants setting the disaster going to be a priced decision |
| Fire burns through a shut door | A shut door with flames within 2 m of the doorway takes one tick of scorch a tick, and at **900** (eighteen seconds) it is gone for good, falling away from the fire. It darkens as it goes, using the same leaf colour a battered door already used, so the failure is visible before it happens | A shut door stopped fire outright and for ever, so a building with its doors closed had rooms nothing could ever reach and shutting yourself in was a way to win. Eighteen seconds is longer than a chair (three) or a table (five), because a door is a slab in a frame, and short enough that it only ever buys time. Only floor fire counts, not a burning chair leaning against the door | A door beside a bonfire reads wrong because the chair against it is ignored, or playtests say the reprieve is too long or too short |
| When a round ends | Everybody out or dead, **or** the whole building doing nothing at all for **1500 ticks** (thirty seconds). "Nothing at all" means nobody moved 400 mm, nobody is on their feet and moving, **nobody is pressing forward and getting nowhere**, nobody is working at a door or dragging somebody or fighting the fire, nobody is alight, and the fire, the burning things and every door are unchanged | The round used to stop as soon as everybody left was out of the fire's reach, which ended it on top of people still walking to the door — and with doors that now burn through, "out of reach" is temporary anyway. **The owner's instruction was that a queue must never end a round**, which is why blocked-but-trying is one of the signals: a crowd jammed in a doorway covers almost no ground, and distance alone would have read that as a settled building | Playtests find a case where the building is genuinely finished but something keeps the clock alive, or a round that should have ended is still running |
| Fire remembers a door can open | A burning square is retired for good once everything around it is alight or walled off. That was safe while a shut door stopped fire for ever; it is not now. Every door that becomes a way through -- opened, forced, blasted or burnt through -- wakes the retired squares in the rooms on both sides of it | Found by the burn-through test: fire that had filled the storage closet stayed in it for the rest of the run however wide the door was afterwards thrown. The bug was always reachable (somebody opening a door onto a fire pressed against it) and burning doors only made it common | Never, while squares are retired at all |
| Alive inside at the end (**owner**) | Still counts as saved, unchanged | The owner chose it. It keeps the score meaning "how many were alive at the end", which is easy to explain and forgiving while the prototype is rough | The owner wants getting people *out* to be the only thing that scores |
| Nobody shuts a door on their own way out | A person never pulls shut a door that stands on the route to the way out they have settled on — **unless the flames have already reached it**, in which case that route was never going to work and shutting it may save them. Somebody with no way out left shuts doors freely | People stopped on their way out to pull doors shut and then dithered, because shutting a door also crosses it off their own list of ways out. The exception is deliberate: shutting the fire out of the room you are cornered in is the mechanic **the owner said he likes and wants kept** | A playtest shows people failing to contain a fire they plainly should have |
| Who slams a door behind them (**owner**) | Evil **8**, raised from 7. Locking it still needs Evil 9 | Four of the twenty authored people cleared the old bar, so the building read as full of door-slammers rather than as containing one. The owner chose 8. No authored personality was changed | The owner wants it rarer still, which is one number, or wants the trait spread itself retuned |
| Furniture never breaks | Chairs and tables are shoved, tipped, rolled and flipped like any other body, and are still furniture when they stop. The table-smashing path and the reserved wreck heaps are gone; chairs simply have no breaking point. The generic wrecking machinery **stays**, because the electrical things that go off still mangle themselves and nobody asked for that to change | The owner asked for table and chair destruction removed. Tables were already real rigid bodies that shove, tip and flip, so this is the removal of a path rather than the building of one | A stone wants breakable scenery on purpose, which would author it rather than making all furniture breakable |
| A chair is furniture, not clutter | Anything people sit on is excluded from calm tidying, by the `CanBeSatOn` row the settings table already had. A frightened person can still wedge one against a door | Tidying is offered before sitting, and in an office the nearest liftable thing is almost always a chair, so the room spent its day carrying its own chairs about and nobody ever sat down | A scenario wants furniture moved on purpose, such as a room being cleared |
| Aiming a card at a person | The pointer is matched against where each body is **drawn on the screen**, within 45 pixels of the middle of the capsule, rather than against a spot on the floor | Cards were aimed by intersecting the ground plane and looking for somebody within half a metre of that point. A body is drawn a metre in the air, so with the camera tilted low the floor under somebody's chest is two or three metres behind their feet: the player clicked a person and the game searched empty carpet. Place-aimed cards still read the floor, which for them is correct | The view stops being a single camera, or people stop being drawn as one capsule |
| Seated bodies | The same body, unsquashed, with its middle dropped to 0.30 m and leaning 12 degrees back. What goes below the floor line is inside the chair | A seated body was flattened to two-thirds height at full width, which turns a capsule into a hunched blob, and then stood on the 0.45 m seat as well — so a sitting head ended up *higher* than a standing one | People stop being capsules |
| Leader and follower marks | A green star over somebody other people are following. Whoever is following them wears nothing | Both wore the same green arrow at different sizes, so a leader and their followers looked alike at a glance. One mark in a knot of people is the one worth looking at | Following needs to be readable on its own, rather than by watching who the star is moving toward |
| Where the instructions live | Every key reminder and the guide to the marks moved off the running screen and into a panel shown only while paused | Attention aids are deliberately thin in this game (see the row above on helper markers), and three rows of instructions left very little of the office to look at. Pause is the moment somebody is reading rather than playing | The pause panel grows past one screenful |
| Reading the round back | A scrollable list on the end card, built from the causal log the run already keeps. Background chatter — the fire creeping a square, an extinguisher hissing, people bumping shoulders — is folded into one line apiece with a count; a button unfolds everything. People are named by the number over their head, doors by the number the hover hint uses | **The owner chose** the story over the raw list. Unfolded, a round runs to thousands of lines and the fire's own spreading buries everything a person did. Presentation only: it reads the log and decides nothing | The retelling stone, which will want to say what a stretch of the log *meant* rather than list it |
| Compatibility version 39 | Bumped from 38, content revision 46 to 47, and all ten replay fingerprints re-recorded | Six of the rows above change what a run produces. Done in the same commit, as [the workflow](development-workflow.md) requires | Every behaviour change; the procedure is the point, not this instance |


## Prototype 2 decision: a camera you can swing, and the log that never opened

Chosen on 2026-09-23 on the owner's behalf, after the owner asked for a camera
that turns under the mouse. None of these is a rule the simulation obeys, so
none of them touches a replay: this is all presentation.

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| Free rotation, kept | Holding the **right mouse button** and dragging swings the view to any angle. Letting go does **not** spring back to a corner | The view could only sit at one of four corners, so anything behind a wall could not be leaned around — the nearest corner was as close as the player could get. Springing back on release would undo the one thing the drag is for | The owner finds the view drifting off-square annoying in ordinary play, in which case a gentle pull back toward the nearest corner after a pause is the smaller fix |
| A drag is a quarter of a degree per pixel | `CameraRig.DegreesPerDragPixel = 0.25f` — a full turn is about a screen and a half of pointer travel | Fine enough to aim at one desk, coarse enough that turning right round is not a chore. Chosen by feel, not measured | Playtesters overshoot constantly, or say turning round takes too long |
| Q and E measure from where the view is **going** | Each press lands on the next corner view round — `45 + 90n` — measured from the angle the view is easing toward rather than the angle it has reached | Measuring from the shown angle loses the second of two quick taps to the first one's travel, so a double tap turned one corner and a bit instead of two | Never, while the view eases at all |
| Five pixels tells a click from a drag | A right button that travelled under `CameraRig.DragPixels = 5f` before coming back up is a click and puts the card down; more than that is a turn and leaves the card in hand. The card is dropped on **release**, not on press | The right button already meant "put the card down", and a drag that also threw the card away would be unusable. At the moment of pressing nobody yet knows which one it is, so the decision has to wait for the button to come back up | A shaky hand drops cards it meant to keep, or a small deliberate nudge of the view throws one away |
| The zoom tilt waits until halfway | The camera holds the isometric 35.264 degrees for the first half of the wheel's travel and eases the whole way to 18 degrees over the second half, smoothed at both ends (`CameraRig.SwoopAt`) | The tilt used to start at the first notch, so a single click of the wheel both moved and tipped the view and read as a lurch rather than a step closer. Coming straight in first makes a small zoom feel like a small zoom | Playtests say the swoop is too sudden when it does arrive, which would move the halfway point rather than change the shape |
| The round read-back is owned by the presentation | `RoundScreens` sets `WantsTheLog`; `FireReactionPrototypePresentation` takes it, clears it, opens `EventLogScreen` and draws it last | The flag was set and never read, so the end card's **What happened** button did nothing at all. Drawing it last is what puts the story over the end card rather than under it | A second screen wants the same treatment, at which point the request flag becomes a small stack |
| The log is for looking, not acting | While the log is up the pointer reaches nothing in the run, and Escape closes it | The same rule pause already follows, and a click meant for the list should never also unlock a door behind it | Never |

## Prototype 2 decision: the new floor plan

Chosen on 2026-09-23. The owner chose the shape of the floor and where the fuse
box sits relative to the way out; everything below was chosen on their behalf.

| Item | Decision | Why now | Revisit when |
| --- | --- | --- | --- |
| The floor plan (**owner**) | A corridor past four rooms, Ting at one end: the way out up one arm, a dead end down the other, and the maintenance room at the far end of the building | The owner asked for four rooms and a corridor, and chose the fuse box far from the way out so that reaching it is a deliberate trip to the back of the floor | Playtests say the single corridor reads as a queue rather than as tension |
| The office and the closet do not move | Room 5001 keeps `(-6000..6000, -6000..6000)` and stays `Rooms[0]`; room 5002 keeps its rectangle and door `2002` | `Rooms[0]` is load-bearing in three places: the fire's spawn area is measured against it, the geometry treats the first room as where the fire starts, and the replay fingerprint kicks boxes at its centre. Beyond that, twenty-seven test files write coordinates inside the office longhand | A room genuinely has to move, at which point the `TheBuilding` test helper is the thing to widen first |
| Door `2008` keeps its number | The one locked way out is still door `2008`, moved from the meeting room's east wall to the north end of the T | The replay fingerprint helper opens `2008` by name, so holding the ID steady is what lets recorded runs stay comparable across the move | Never, while `2008` means "the way out" |
| Archways | A new `IsOpening` flag on a door definition makes it a permanent opening: `IsHole`, `Placed`, and `Broken` from tick zero. It reuses the machinery a blown-open wall already had | A T-shaped corridor is two rectangles, and joining two rooms required a door. A swinging door in the middle of a hallway is wrong, and a wall there would have made the T impossible | A level wants an opening that can be closed, which is a different thing and should stay a different thing |
| The archway is 2.4 m of a 3 m corridor | Not the full width | A doorway has to leave a body's width of wall at each side or the validator refuses it, so an opening can never be the entire wall | The rule about stubs of wall changes |
| Stalls are 1.5 m square with 800 mm doors | Each stall is its own room off the bathroom's south wall | The engine's stated minimum door is 600 mm, but a 600 mm gap fails the navigation check that actually decides whether a body fits, because clearance is measured from square centres. 800 mm clears it in the worst alignment by about 50 mm, so it survives the building being moved | Navigation squares change size |
| The fire's spawn area may be any room | `Validate` used to require the rectangle inside `Rooms[0]`; it now requires it inside *some* one room, clear of the walls | The danger is about to be allowed to start anywhere on the floor, and the first room is no longer special | Never |
| Content revision, not compatibility version | `ContentRevision` 47 -> 48; `SimulationCompatibilityVersion` stays 39 | `docs/simulation-contract.md` reserves the compatibility version for changes to the *rules* -- the step, the command envelope, the generator, the tick schedule, the meaning of an event. A building is content, which is what the content revision is for. The ten fingerprints were re-recorded either way, because they are recorded against this scenario's content | A change to the floor plan ever changes a rule, which it should not |
| Where the danger may start (**owner**) | Any of the four rooms, never the corridor. `FireSettings.SpawnBounds` (one rectangle) became `SpawnAreas` (a list), with one area well inside each room | The owner asked for the fire to start in more randomised places and said a finished level would have a preset area for it; a list of preset areas is that idea, and a level that wants one spot still names one | The owner wants a room excluded, or the corridor included after all |
| One area draws nothing for the choice | `FireSystem` skips the "which area" draw when a scenario names exactly one, rather than making it and throwing it away | Nearly every test pins the fire to one spot. Drawing and discarding would have shifted every one of their runs for no reason, because the generator advances whether or not the answer could vary | Never; a draw whose answer is known is not a draw |
| The drawn spot is nudged onto floor | A point that lands on a square whose middle is in a wall walks outward, ring by ring in a fixed order, to the nearest square that is floor in some room. No random numbers | A spawn area is a rectangle, and a rectangle inside a room can still contain squares whose middles fall in the brickwork. A fire lit there sits in a wall doing nothing | Never |
| The cable is authored, not worked out | Each run is a list of corners in whole millimetres on `FireReactionPowerLineDefinition`. The run measures its own length from them; the view draws the spark at the same fraction along the same corners | How long the spark takes is an outcome of the round -- it decides when the next socket pops -- and where the spark is drawn is a picture. With one route and one number they cannot drift apart | A level wants cable it did not draw by hand, at which point routing it becomes a tool rather than a rule |
| Cable belongs to the things it joins | A run whose ends are not in this building is simply not there, rather than being refused | Dozens of tests replace the clutter with two boxes and a chair. Making each of them also delete the wiring would be churn for no safety: cable to a socket that does not exist is not a mistake, it is an absence | A building wants wiring to something that is not a socket or a fuse box, which would be a mistake |
| The spark crawls at about two metres a second | `PowerSettings.SparkSpeedMillimetresPerTick = 45`, down from 120 | At six metres a second the spark outran everybody in the building: the whole chain went off within seconds and a test round with every door open lost nineteen of twenty people and saved none. At the slower speed the same round saves nine. A fuse that outruns the people watching it is a delayed explosion, not a fuse | Playtests say the wait is boring rather than tense |
| A new phase in the tick, and a new compatibility version | `power.Advance()` runs in phase 2, immediately after `fire.Advance()`. `SimulationCompatibilityVersion` 39 -> 40 | `docs/simulation-contract.md` names the tick schedule as one of the things that forces the version. Going after the fire leaves the fire's random draws exactly where they were, so the only replay movement comes from the cable actually doing something | Never; a phase's place in the tick is part of the contract |
| The fuse box card aims at a place | `PlayerCommandType.PopFuseBox` carries a point, and the card finds a fuse box within 2.5 m of it | The pointer already has a place path for TNT and the extinguisher, and a floor has one fuse box. Aiming at a thing would have meant a third way of picking targets for no gain | A building has more than one fuse box |
| A refused card writes nothing | The reach is checked before the event is appended, not after | The log only ever grows, so it cannot record something and then take it back. A card that does nothing must leave no trace, the way every other refused card does | Never |
| A menu command the bridge drives never asks | `RewriteScenarioAsset` checks `SessionState` for the flag the test bridge sets, and takes the yes as given | Driving that command from a script put a modal dialog up in an editor nobody was watching, which froze Unity -- not just the command, but every later request and recompiling with it -- until somebody noticed | Never; asking somebody who is not there is worse than not asking |

## How decisions are made

Paniq's owner is learning game development, so technical decisions must remain
teachable and reversible. Before adding a package, workflow, or major system:

1. Research the current official documentation and inspect the existing project.
2. State the problem in game terms, then give one plain-language recommendation.
3. Record why it is needed now, what simpler option was retained or rejected,
   relevant source links, and the condition that would trigger a later review.
4. Explain the implementation result and exact local Unity steps needed to
   verify it.

Ask the owner only to choose genuine product direction, such as the intended
player experience or visual tone - not engine settings they have no reason to
know yet.

## Design constraints for future expansion

The authoritative rules for replayable simulation are in
[Simulation contract](simulation-contract.md). They apply before any gameplay
system is introduced.

- Keep interaction data explicit: source ID, event type, world position,
  strength, duration, and causal parent.
- Keep configuration in scenes and ScriptableObjects rather than hidden static
  state.
- Keep random choices tied to an explicit scenario seed.
- Keep presentation separate from future gameplay/simulation decisions.
- Prefer simple steering and authored obstacles before adding navigation.

## Deferred technology

| Technology | Do not add it until |
| --- | --- |
| ECS, Burst, Entities Graphics | A profiled representative prototype scene cannot meet its frame-rate target because of crowd update or rendering cost. |
| AI Navigation | A prototype stone needs pathfinding that simple steering and obstacle avoidance cannot provide. |
| Cinemachine | The hand-authored camera prevents a required player experience. |
| Addressables | Content size, loading needs, or platform packaging makes direct references difficult to manage. |
| Mobile settings | The Windows prototype is stable and a target device is available for measurement. |
| Unity Physics (DOTS) | Standalone profiling shows the PhysX step over 3 ms a tick at 500 people and 1,000 things after the cheaper fixes, or replays must match across different kinds of computer. |
| VFX Graph | The built-in Particle System costs more than 2 ms a frame in the worst blast, or an effect needs tens of thousands of particles, such as smoke filling a building. |

Any addition above requires a short dated note here recording the measured or
feature-driven reason and the expected benefit.
